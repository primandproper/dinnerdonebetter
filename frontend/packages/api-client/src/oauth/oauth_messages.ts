// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: oauth/oauth_messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../google/protobuf/timestamp';

export const protobufPackage = 'oauth';

export interface OAuth2Client {
  createdAt: Date | undefined;
  archivedAt: Date | undefined;
  name: string;
  description: string;
  clientId: string;
  id: string;
  clientSecret: string;
  /**
   * The exact addresses this client may receive an authorization code at, compared byte for
   * byte by the authorization server. A client registers what it will actually send as
   * redirect_uri, ports and trailing slashes included.
   */
  redirectUris: string[];
}

function createBaseOAuth2Client(): OAuth2Client {
  return {
    createdAt: undefined,
    archivedAt: undefined,
    name: '',
    description: '',
    clientId: '',
    id: '',
    clientSecret: '',
    redirectUris: [],
  };
}

export const OAuth2Client: MessageFns<OAuth2Client> = {
  encode(message: OAuth2Client, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(18).fork()).join();
    }
    if (message.name !== '') {
      writer.uint32(26).string(message.name);
    }
    if (message.description !== '') {
      writer.uint32(34).string(message.description);
    }
    if (message.clientId !== '') {
      writer.uint32(42).string(message.clientId);
    }
    if (message.id !== '') {
      writer.uint32(50).string(message.id);
    }
    if (message.clientSecret !== '') {
      writer.uint32(58).string(message.clientSecret);
    }
    for (const v of message.redirectUris) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OAuth2Client {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOAuth2Client();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.clientSecret = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.redirectUris.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OAuth2Client {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
          ? globalThis.String(object.client_id)
          : '',
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      clientSecret: isSet(object.clientSecret)
        ? globalThis.String(object.clientSecret)
        : isSet(object.client_secret)
          ? globalThis.String(object.client_secret)
          : '',
      redirectUris: globalThis.Array.isArray(object?.redirectUris)
        ? object.redirectUris.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.redirect_uris)
          ? object.redirect_uris.map((e: any) => globalThis.String(e))
          : [],
    };
  },

  toJSON(message: OAuth2Client): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.description !== '') {
      obj.description = message.description;
    }
    if (message.clientId !== '') {
      obj.clientId = message.clientId;
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.clientSecret !== '') {
      obj.clientSecret = message.clientSecret;
    }
    if (message.redirectUris?.length) {
      obj.redirectUris = message.redirectUris;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OAuth2Client>, I>>(base?: I): OAuth2Client {
    return OAuth2Client.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OAuth2Client>, I>>(object: I): OAuth2Client {
    const message = createBaseOAuth2Client();
    message.createdAt = object.createdAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.name = object.name ?? '';
    message.description = object.description ?? '';
    message.clientId = object.clientId ?? '';
    message.id = object.id ?? '';
    message.clientSecret = object.clientSecret ?? '';
    message.redirectUris = object.redirectUris?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
