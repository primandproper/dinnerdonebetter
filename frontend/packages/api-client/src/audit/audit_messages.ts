// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: audit/audit_messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../google/protobuf/timestamp';

export const protobufPackage = 'audit';

export interface DataCollection {
  accountAuditLogEntries: { [key: string]: AuditLogEntry };
  userAuditLogEntries: AuditLogEntry[];
}

export interface DataCollection_AccountAuditLogEntriesEntry {
  key: string;
  value: AuditLogEntry | undefined;
}

/**
 * ChangeLog is one field's before and after.
 *
 * The values are rendered strings rather than a typed Value because that is what
 * they have always been on this wire, and a client that shows an audit trail
 * shows text. The stored form is typed; the rendering happens at the edge.
 */
export interface ChangeLog {
  oldValue: string;
  newValue: string;
}

export interface AuditLogEntry {
  createdAt: Date | undefined;
  changes: { [key: string]: ChangeLog };
  belongsToAccount: string;
  id: string;
  resourceType: string;
  relevantId: string;
  eventType: string;
  belongsToUser: string;
  /**
   * What kind of principal acted, and where from. Recorded rather than derived at
   * read time: the association between a principal and an address is what an
   * investigation needs and is not recoverable afterwards.
   */
  actorType: string;
  actorIp: string;
  /**
   * The hash chain. Entries chain per scope, each carrying its predecessor's hash
   * and its own hash over that plus its own contents, so any edit, removal or
   * reordering breaks the chain at a nameable position.
   *
   * These are exposed so a caller can check the chain without trusting this
   * service to check it for them — which is the only version of the check worth
   * much. A clean walk proves nobody altered an entry without also rewriting every
   * entry after it; it does not prove the table was not replaced wholesale, and
   * nothing self-contained can. Publishing hash somewhere this database's owner
   * does not control is the answer to that.
   */
  scope: string;
  prevHash: string;
  hash: string;
  seq: number;
}

export interface AuditLogEntry_ChangesEntry {
  key: string;
  value: ChangeLog | undefined;
}

function createBaseDataCollection(): DataCollection {
  return { accountAuditLogEntries: {}, userAuditLogEntries: [] };
}

export const DataCollection: MessageFns<DataCollection> = {
  encode(message: DataCollection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.accountAuditLogEntries).forEach(([key, value]: [string, AuditLogEntry]) => {
      DataCollection_AccountAuditLogEntriesEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    for (const v of message.userAuditLogEntries) {
      AuditLogEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = DataCollection_AccountAuditLogEntriesEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.accountAuditLogEntries[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userAuditLogEntries.push(AuditLogEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection {
    return {
      accountAuditLogEntries: isObject(object.accountAuditLogEntries)
        ? (globalThis.Object.entries(object.accountAuditLogEntries) as [string, any][]).reduce(
            (acc: { [key: string]: AuditLogEntry }, [key, value]: [string, any]) => {
              acc[key] = AuditLogEntry.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.account_audit_log_entries)
          ? (globalThis.Object.entries(object.account_audit_log_entries) as [string, any][]).reduce(
              (acc: { [key: string]: AuditLogEntry }, [key, value]: [string, any]) => {
                acc[key] = AuditLogEntry.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
      userAuditLogEntries: globalThis.Array.isArray(object?.userAuditLogEntries)
        ? object.userAuditLogEntries.map((e: any) => AuditLogEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.user_audit_log_entries)
          ? object.user_audit_log_entries.map((e: any) => AuditLogEntry.fromJSON(e))
          : [],
    };
  },

  toJSON(message: DataCollection): unknown {
    const obj: any = {};
    if (message.accountAuditLogEntries) {
      const entries = globalThis.Object.entries(message.accountAuditLogEntries) as [string, AuditLogEntry][];
      if (entries.length > 0) {
        obj.accountAuditLogEntries = {};
        entries.forEach(([k, v]) => {
          obj.accountAuditLogEntries[k] = AuditLogEntry.toJSON(v);
        });
      }
    }
    if (message.userAuditLogEntries?.length) {
      obj.userAuditLogEntries = message.userAuditLogEntries.map((e) => AuditLogEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection>, I>>(base?: I): DataCollection {
    return DataCollection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection>, I>>(object: I): DataCollection {
    const message = createBaseDataCollection();
    message.accountAuditLogEntries = (
      globalThis.Object.entries(object.accountAuditLogEntries ?? {}) as [string, AuditLogEntry][]
    ).reduce((acc: { [key: string]: AuditLogEntry }, [key, value]: [string, AuditLogEntry]) => {
      if (value !== undefined) {
        acc[key] = AuditLogEntry.fromPartial(value);
      }
      return acc;
    }, {});
    message.userAuditLogEntries = object.userAuditLogEntries?.map((e) => AuditLogEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDataCollection_AccountAuditLogEntriesEntry(): DataCollection_AccountAuditLogEntriesEntry {
  return { key: '', value: undefined };
}

export const DataCollection_AccountAuditLogEntriesEntry: MessageFns<DataCollection_AccountAuditLogEntriesEntry> = {
  encode(message: DataCollection_AccountAuditLogEntriesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      AuditLogEntry.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection_AccountAuditLogEntriesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection_AccountAuditLogEntriesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = AuditLogEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection_AccountAuditLogEntriesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? AuditLogEntry.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: DataCollection_AccountAuditLogEntriesEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = AuditLogEntry.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection_AccountAuditLogEntriesEntry>, I>>(
    base?: I,
  ): DataCollection_AccountAuditLogEntriesEntry {
    return DataCollection_AccountAuditLogEntriesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection_AccountAuditLogEntriesEntry>, I>>(
    object: I,
  ): DataCollection_AccountAuditLogEntriesEntry {
    const message = createBaseDataCollection_AccountAuditLogEntriesEntry();
    message.key = object.key ?? '';
    message.value =
      object.value !== undefined && object.value !== null ? AuditLogEntry.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseChangeLog(): ChangeLog {
  return { oldValue: '', newValue: '' };
}

export const ChangeLog: MessageFns<ChangeLog> = {
  encode(message: ChangeLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oldValue !== '') {
      writer.uint32(10).string(message.oldValue);
    }
    if (message.newValue !== '') {
      writer.uint32(18).string(message.newValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangeLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangeLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oldValue = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newValue = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChangeLog {
    return {
      oldValue: isSet(object.oldValue)
        ? globalThis.String(object.oldValue)
        : isSet(object.old_value)
          ? globalThis.String(object.old_value)
          : '',
      newValue: isSet(object.newValue)
        ? globalThis.String(object.newValue)
        : isSet(object.new_value)
          ? globalThis.String(object.new_value)
          : '',
    };
  },

  toJSON(message: ChangeLog): unknown {
    const obj: any = {};
    if (message.oldValue !== '') {
      obj.oldValue = message.oldValue;
    }
    if (message.newValue !== '') {
      obj.newValue = message.newValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChangeLog>, I>>(base?: I): ChangeLog {
    return ChangeLog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChangeLog>, I>>(object: I): ChangeLog {
    const message = createBaseChangeLog();
    message.oldValue = object.oldValue ?? '';
    message.newValue = object.newValue ?? '';
    return message;
  },
};

function createBaseAuditLogEntry(): AuditLogEntry {
  return {
    createdAt: undefined,
    changes: {},
    belongsToAccount: '',
    id: '',
    resourceType: '',
    relevantId: '',
    eventType: '',
    belongsToUser: '',
    actorType: '',
    actorIp: '',
    scope: '',
    prevHash: '',
    hash: '',
    seq: 0,
  };
}

export const AuditLogEntry: MessageFns<AuditLogEntry> = {
  encode(message: AuditLogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    globalThis.Object.entries(message.changes).forEach(([key, value]: [string, ChangeLog]) => {
      AuditLogEntry_ChangesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.belongsToAccount !== '') {
      writer.uint32(26).string(message.belongsToAccount);
    }
    if (message.id !== '') {
      writer.uint32(34).string(message.id);
    }
    if (message.resourceType !== '') {
      writer.uint32(42).string(message.resourceType);
    }
    if (message.relevantId !== '') {
      writer.uint32(50).string(message.relevantId);
    }
    if (message.eventType !== '') {
      writer.uint32(58).string(message.eventType);
    }
    if (message.belongsToUser !== '') {
      writer.uint32(66).string(message.belongsToUser);
    }
    if (message.actorType !== '') {
      writer.uint32(74).string(message.actorType);
    }
    if (message.actorIp !== '') {
      writer.uint32(82).string(message.actorIp);
    }
    if (message.scope !== '') {
      writer.uint32(90).string(message.scope);
    }
    if (message.prevHash !== '') {
      writer.uint32(98).string(message.prevHash);
    }
    if (message.hash !== '') {
      writer.uint32(106).string(message.hash);
    }
    if (message.seq !== 0) {
      writer.uint32(112).int64(message.seq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditLogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = AuditLogEntry_ChangesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.changes[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.belongsToAccount = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resourceType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.relevantId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.belongsToUser = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.actorType = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.actorIp = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.scope = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.prevHash = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.seq = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditLogEntry {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      changes: isObject(object.changes)
        ? (globalThis.Object.entries(object.changes) as [string, any][]).reduce(
            (acc: { [key: string]: ChangeLog }, [key, value]: [string, any]) => {
              acc[key] = ChangeLog.fromJSON(value);
              return acc;
            },
            {},
          )
        : {},
      belongsToAccount: isSet(object.belongsToAccount)
        ? globalThis.String(object.belongsToAccount)
        : isSet(object.belongs_to_account)
          ? globalThis.String(object.belongs_to_account)
          : '',
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      resourceType: isSet(object.resourceType)
        ? globalThis.String(object.resourceType)
        : isSet(object.resource_type)
          ? globalThis.String(object.resource_type)
          : '',
      relevantId: isSet(object.relevantId)
        ? globalThis.String(object.relevantId)
        : isSet(object.relevant_id)
          ? globalThis.String(object.relevant_id)
          : '',
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
          ? globalThis.String(object.event_type)
          : '',
      belongsToUser: isSet(object.belongsToUser)
        ? globalThis.String(object.belongsToUser)
        : isSet(object.belongs_to_user)
          ? globalThis.String(object.belongs_to_user)
          : '',
      actorType: isSet(object.actorType)
        ? globalThis.String(object.actorType)
        : isSet(object.actor_type)
          ? globalThis.String(object.actor_type)
          : '',
      actorIp: isSet(object.actorIp)
        ? globalThis.String(object.actorIp)
        : isSet(object.actor_ip)
          ? globalThis.String(object.actor_ip)
          : '',
      scope: isSet(object.scope) ? globalThis.String(object.scope) : '',
      prevHash: isSet(object.prevHash)
        ? globalThis.String(object.prevHash)
        : isSet(object.prev_hash)
          ? globalThis.String(object.prev_hash)
          : '',
      hash: isSet(object.hash) ? globalThis.String(object.hash) : '',
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
    };
  },

  toJSON(message: AuditLogEntry): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.changes) {
      const entries = globalThis.Object.entries(message.changes) as [string, ChangeLog][];
      if (entries.length > 0) {
        obj.changes = {};
        entries.forEach(([k, v]) => {
          obj.changes[k] = ChangeLog.toJSON(v);
        });
      }
    }
    if (message.belongsToAccount !== '') {
      obj.belongsToAccount = message.belongsToAccount;
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.resourceType !== '') {
      obj.resourceType = message.resourceType;
    }
    if (message.relevantId !== '') {
      obj.relevantId = message.relevantId;
    }
    if (message.eventType !== '') {
      obj.eventType = message.eventType;
    }
    if (message.belongsToUser !== '') {
      obj.belongsToUser = message.belongsToUser;
    }
    if (message.actorType !== '') {
      obj.actorType = message.actorType;
    }
    if (message.actorIp !== '') {
      obj.actorIp = message.actorIp;
    }
    if (message.scope !== '') {
      obj.scope = message.scope;
    }
    if (message.prevHash !== '') {
      obj.prevHash = message.prevHash;
    }
    if (message.hash !== '') {
      obj.hash = message.hash;
    }
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuditLogEntry>, I>>(base?: I): AuditLogEntry {
    return AuditLogEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuditLogEntry>, I>>(object: I): AuditLogEntry {
    const message = createBaseAuditLogEntry();
    message.createdAt = object.createdAt ?? undefined;
    message.changes = (globalThis.Object.entries(object.changes ?? {}) as [string, ChangeLog][]).reduce(
      (acc: { [key: string]: ChangeLog }, [key, value]: [string, ChangeLog]) => {
        if (value !== undefined) {
          acc[key] = ChangeLog.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.belongsToAccount = object.belongsToAccount ?? '';
    message.id = object.id ?? '';
    message.resourceType = object.resourceType ?? '';
    message.relevantId = object.relevantId ?? '';
    message.eventType = object.eventType ?? '';
    message.belongsToUser = object.belongsToUser ?? '';
    message.actorType = object.actorType ?? '';
    message.actorIp = object.actorIp ?? '';
    message.scope = object.scope ?? '';
    message.prevHash = object.prevHash ?? '';
    message.hash = object.hash ?? '';
    message.seq = object.seq ?? 0;
    return message;
  },
};

function createBaseAuditLogEntry_ChangesEntry(): AuditLogEntry_ChangesEntry {
  return { key: '', value: undefined };
}

export const AuditLogEntry_ChangesEntry: MessageFns<AuditLogEntry_ChangesEntry> = {
  encode(message: AuditLogEntry_ChangesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ChangeLog.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditLogEntry_ChangesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditLogEntry_ChangesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ChangeLog.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditLogEntry_ChangesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? ChangeLog.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: AuditLogEntry_ChangesEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ChangeLog.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuditLogEntry_ChangesEntry>, I>>(base?: I): AuditLogEntry_ChangesEntry {
    return AuditLogEntry_ChangesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuditLogEntry_ChangesEntry>, I>>(object: I): AuditLogEntry_ChangesEntry {
    const message = createBaseAuditLogEntry_ChangesEntry();
    message.key = object.key ?? '';
    message.value =
      object.value !== undefined && object.value !== null ? ChangeLog.fromPartial(object.value) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER');
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER');
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
