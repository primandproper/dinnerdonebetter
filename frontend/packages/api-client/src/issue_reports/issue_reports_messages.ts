// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: issue_reports/issue_reports_messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../google/protobuf/timestamp';

export const protobufPackage = 'issue_reports';

export interface DataCollection {
  issueReports: { [key: string]: IssueReport };
}

export interface DataCollection_IssueReportsEntry {
  key: string;
  value: IssueReport | undefined;
}

/**
 * IssueReport is one thing somebody told us was wrong, and where that report
 * stands.
 */
export interface IssueReport {
  id: string;
  /** reporter is the ID of the user who filed it. */
  reporter: string;
  /**
   * kind is the category the report was filed under — bug, feature_request,
   * data_quality, performance, other. A triage queue groups and routes by it.
   */
  kind: string;
  /** details is what the person actually said. */
  details: string;
  /**
   * subject_type is what the report is about, as the owning domain spells it.
   * Empty is a report about the product in general.
   */
  subjectType: string;
  /**
   * subject_id is which one of them. Empty is a report about a kind of thing
   * rather than about one of them.
   */
  subjectId: string;
  /**
   * status is where the report stands: open, acknowledged, resolved or
   * declined. It moves only through TransitionIssueReport.
   */
  status: string;
  /**
   * resolution is why the report is in the terminal status it is in — the note
   * a triager left when they resolved or declined it. Reopening clears it.
   */
  resolution: string;
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
  /**
   * closed_at is when the report reached a terminal status, and unset while it
   * is still open or acknowledged. It is what a time-to-resolution number is
   * measured to; a boolean beside status would not answer "when".
   */
  closedAt: Date | undefined;
}

function createBaseDataCollection(): DataCollection {
  return { issueReports: {} };
}

export const DataCollection: MessageFns<DataCollection> = {
  encode(message: DataCollection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.issueReports).forEach(([key, value]: [string, IssueReport]) => {
      DataCollection_IssueReportsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = DataCollection_IssueReportsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.issueReports[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection {
    return {
      issueReports: isObject(object.issueReports)
        ? (globalThis.Object.entries(object.issueReports) as [string, any][]).reduce(
            (acc: { [key: string]: IssueReport }, [key, value]: [string, any]) => {
              acc[key] = IssueReport.fromJSON(value);
              return acc;
            },
            {},
          )
        : isObject(object.issue_reports)
          ? (globalThis.Object.entries(object.issue_reports) as [string, any][]).reduce(
              (acc: { [key: string]: IssueReport }, [key, value]: [string, any]) => {
                acc[key] = IssueReport.fromJSON(value);
                return acc;
              },
              {},
            )
          : {},
    };
  },

  toJSON(message: DataCollection): unknown {
    const obj: any = {};
    if (message.issueReports) {
      const entries = globalThis.Object.entries(message.issueReports) as [string, IssueReport][];
      if (entries.length > 0) {
        obj.issueReports = {};
        entries.forEach(([k, v]) => {
          obj.issueReports[k] = IssueReport.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection>, I>>(base?: I): DataCollection {
    return DataCollection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection>, I>>(object: I): DataCollection {
    const message = createBaseDataCollection();
    message.issueReports = (globalThis.Object.entries(object.issueReports ?? {}) as [string, IssueReport][]).reduce(
      (acc: { [key: string]: IssueReport }, [key, value]: [string, IssueReport]) => {
        if (value !== undefined) {
          acc[key] = IssueReport.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseDataCollection_IssueReportsEntry(): DataCollection_IssueReportsEntry {
  return { key: '', value: undefined };
}

export const DataCollection_IssueReportsEntry: MessageFns<DataCollection_IssueReportsEntry> = {
  encode(message: DataCollection_IssueReportsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== '') {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      IssueReport.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataCollection_IssueReportsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataCollection_IssueReportsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = IssueReport.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataCollection_IssueReportsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : '',
      value: isSet(object.value) ? IssueReport.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: DataCollection_IssueReportsEntry): unknown {
    const obj: any = {};
    if (message.key !== '') {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = IssueReport.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataCollection_IssueReportsEntry>, I>>(
    base?: I,
  ): DataCollection_IssueReportsEntry {
    return DataCollection_IssueReportsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataCollection_IssueReportsEntry>, I>>(
    object: I,
  ): DataCollection_IssueReportsEntry {
    const message = createBaseDataCollection_IssueReportsEntry();
    message.key = object.key ?? '';
    message.value =
      object.value !== undefined && object.value !== null ? IssueReport.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseIssueReport(): IssueReport {
  return {
    id: '',
    reporter: '',
    kind: '',
    details: '',
    subjectType: '',
    subjectId: '',
    status: '',
    resolution: '',
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
    closedAt: undefined,
  };
}

export const IssueReport: MessageFns<IssueReport> = {
  encode(message: IssueReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.reporter !== '') {
      writer.uint32(18).string(message.reporter);
    }
    if (message.kind !== '') {
      writer.uint32(26).string(message.kind);
    }
    if (message.details !== '') {
      writer.uint32(34).string(message.details);
    }
    if (message.subjectType !== '') {
      writer.uint32(42).string(message.subjectType);
    }
    if (message.subjectId !== '') {
      writer.uint32(50).string(message.subjectId);
    }
    if (message.status !== '') {
      writer.uint32(58).string(message.status);
    }
    if (message.resolution !== '') {
      writer.uint32(66).string(message.resolution);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(74).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(82).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(90).fork()).join();
    }
    if (message.closedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closedAt), writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssueReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssueReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reporter = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.details = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.subjectType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.resolution = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.closedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssueReport {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      reporter: isSet(object.reporter) ? globalThis.String(object.reporter) : '',
      kind: isSet(object.kind) ? globalThis.String(object.kind) : '',
      details: isSet(object.details) ? globalThis.String(object.details) : '',
      subjectType: isSet(object.subjectType)
        ? globalThis.String(object.subjectType)
        : isSet(object.subject_type)
          ? globalThis.String(object.subject_type)
          : '',
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
          ? globalThis.String(object.subject_id)
          : '',
      status: isSet(object.status) ? globalThis.String(object.status) : '',
      resolution: isSet(object.resolution) ? globalThis.String(object.resolution) : '',
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      closedAt: isSet(object.closedAt)
        ? fromJsonTimestamp(object.closedAt)
        : isSet(object.closed_at)
          ? fromJsonTimestamp(object.closed_at)
          : undefined,
    };
  },

  toJSON(message: IssueReport): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.reporter !== '') {
      obj.reporter = message.reporter;
    }
    if (message.kind !== '') {
      obj.kind = message.kind;
    }
    if (message.details !== '') {
      obj.details = message.details;
    }
    if (message.subjectType !== '') {
      obj.subjectType = message.subjectType;
    }
    if (message.subjectId !== '') {
      obj.subjectId = message.subjectId;
    }
    if (message.status !== '') {
      obj.status = message.status;
    }
    if (message.resolution !== '') {
      obj.resolution = message.resolution;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.closedAt !== undefined) {
      obj.closedAt = message.closedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IssueReport>, I>>(base?: I): IssueReport {
    return IssueReport.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IssueReport>, I>>(object: I): IssueReport {
    const message = createBaseIssueReport();
    message.id = object.id ?? '';
    message.reporter = object.reporter ?? '';
    message.kind = object.kind ?? '';
    message.details = object.details ?? '';
    message.subjectType = object.subjectType ?? '';
    message.subjectId = object.subjectId ?? '';
    message.status = object.status ?? '';
    message.resolution = object.resolution ?? '';
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.closedAt = object.closedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
