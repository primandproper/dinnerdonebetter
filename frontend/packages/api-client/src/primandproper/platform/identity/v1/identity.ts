// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: primandproper/platform/identity/v1/identity.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from '@grpc/grpc-js';
import { Timestamp } from '../../../../google/protobuf/timestamp';
import { Pagination, QueryFilter } from '../../filtering/v1/filtering';

export const protobufPackage = 'primandproper.platform.identity.v1';

/**
 * Package primandproper.platform.identity.v1 is the wire schema for the four
 * nouns this module's identity package owns -- users, accounts, memberships and
 * invitations -- and for the operations over them that are more than one write.
 *
 * This file is shipped inside the published Go module, and it is the file
 * itself that is shipped -- not a copy for you to keep in sync. A consumer puts
 * the module's proto directory on protoc's path and imports this file by its
 * canonical name, exactly as filtering.proto already works:
 *
 * 	PLATFORM_PROTO := $(shell go list -m -f '{{.Dir}}' github.com/primandproper/platform-go/v14)
 *
 * 	protoc --proto_path proto/ \
 * 	    --proto_path $(PLATFORM_PROTO)/identity/proto \
 * 	    --proto_path $(PLATFORM_PROTO)/filtering/proto \
 * 	    --go_opt=Mprimandproper/platform/identity/v1/identity.proto=github.com/primandproper/platform-go/v14/identity/identitypb \
 * 	    --go_opt=Mprimandproper/platform/filtering/v1/filtering.proto=github.com/primandproper/primitives-go/v2/filtering/filteringpb \
 * 	    $(CONSUMER_PROTO_FILES)   # both platform files deliberately absent from that list
 *
 * Go links against the bindings this module already generated, in
 * github.com/primandproper/platform-go/v14/identity/identitypb, rather than
 * making a second copy: the server and the typed client here are built against
 * those types, and a consumer generating its own would hold types that look
 * identical and satisfy nothing. Swift, TypeScript and Kotlin have no such
 * bindings to link against and generate this file directly, which is the whole
 * point of shipping the schema rather than a Go package.
 *
 * Field numbers are the compatibility promise, and they are the promise across
 * every language a consumer generates into. Numbers are never reused and never
 * repurposed: a field that goes away is reserved.
 *
 * # What is not here, and why
 *
 * No scope field, anywhere, and the name is reserved so there cannot be one.
 * Every row this schema describes carries a tenancy.Scope, and every read
 * filters on it -- but a scope a client could put in a request is a
 * cross-tenant read hiding behind a request field. The scope comes off the
 * principal the consumer's authentication interceptor resolved, and from
 * nowhere else. See identity/grpc for that seam.
 *
 * Reserving the name rather than only saying so is audit.proto's pattern:
 * `reserved "scope";` is a schema protoc refuses to accept a scope field into,
 * in this repository and in a consumer's fork of the file alike, whereas a
 * comment is a request to the next author. It is reserved on all twenty-nine
 * request messages, on the four inputs they are built from, and on the nine
 * messages a response is built from -- a scope on one of those would be
 * answering a client with something the client supplied. The response wrappers
 * hold nothing but those messages and reserve nothing.
 *
 * No credentials, in either direction. There is no hashed_password,
 * two_factor_secret or email_address_verification_token on User, and no token
 * on Invitation -- an invitation's token appears only as a request field on the
 * two RPCs that answer one, because that is where it arrives from, on a link.
 * A schema with no field for a secret is a stronger guarantee than a converter
 * that remembers to clear one.
 *
 * No credential RPCs either: setting a password, enrolling a second factor and
 * verifying an email address are the sign-in service's, not the directory's,
 * and RegisterRequest carries no password for the same reason. The identity
 * package never hashes -- it stores what an engine produced -- so a plaintext
 * password on this wire would put the choice of hashing engine in the
 * transport.
 *
 * Registration here therefore mints the passwordless user that package already
 * treats as first-class. A registration that carries a credential is
 * SignInService.Register, in signin.proto: that service holds the authenticator,
 * hashes what arrives, and comes back through this package's own registration on
 * one transaction. Which of the two a consumer calls is the question of whether
 * the registrant is choosing a password at that moment -- a directory being
 * filled from elsewhere is this one, and somebody signing up is that one.
 *
 * No avatar. The media registry is this module's, but identity has no avatar
 * column and joining one is a contract between two packages that has not been
 * designed. It is absent rather than guessed at.
 */

/**
 * AccountStatus is where a user stands with the operator: whether they may sign
 * in at all, and if not, why not. It is not a permission -- what a user may do
 * once admitted is resolved from their roles.
 */
export enum AccountStatus {
  /**
   * ACCOUNT_STATUS_UNSPECIFIED - Unset. A request naming it is refused rather than defaulted: the Go type is
   * a closed set of four, and guessing which one a caller meant is how a banned
   * user gets reinstated by a client that forgot to set the field.
   */
  ACCOUNT_STATUS_UNSPECIFIED = 0,
  /** ACCOUNT_STATUS_UNVERIFIED - Registered, and has not yet proven whatever registration asked of them. */
  ACCOUNT_STATUS_UNVERIFIED = 1,
  /** ACCOUNT_STATUS_GOOD - In good standing. The only status that admits a sign-in. */
  ACCOUNT_STATUS_GOOD = 2,
  /**
   * ACCOUNT_STATUS_BANNED - Suspended by an operator, reversibly. The explanation is meant to be shown
   * to them.
   */
  ACCOUNT_STATUS_BANNED = 3,
  /**
   * ACCOUNT_STATUS_TERMINATED - Ended permanently. Not erasure -- the row remains, because an account with
   * a payment history and an audit trail cannot simply vanish.
   */
  ACCOUNT_STATUS_TERMINATED = 4,
  UNRECOGNIZED = -1,
}

export function accountStatusFromJSON(object: any): AccountStatus {
  switch (object) {
    case 0:
    case 'ACCOUNT_STATUS_UNSPECIFIED':
      return AccountStatus.ACCOUNT_STATUS_UNSPECIFIED;
    case 1:
    case 'ACCOUNT_STATUS_UNVERIFIED':
      return AccountStatus.ACCOUNT_STATUS_UNVERIFIED;
    case 2:
    case 'ACCOUNT_STATUS_GOOD':
      return AccountStatus.ACCOUNT_STATUS_GOOD;
    case 3:
    case 'ACCOUNT_STATUS_BANNED':
      return AccountStatus.ACCOUNT_STATUS_BANNED;
    case 4:
    case 'ACCOUNT_STATUS_TERMINATED':
      return AccountStatus.ACCOUNT_STATUS_TERMINATED;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return AccountStatus.UNRECOGNIZED;
  }
}

export function accountStatusToJSON(object: AccountStatus): string {
  switch (object) {
    case AccountStatus.ACCOUNT_STATUS_UNSPECIFIED:
      return 'ACCOUNT_STATUS_UNSPECIFIED';
    case AccountStatus.ACCOUNT_STATUS_UNVERIFIED:
      return 'ACCOUNT_STATUS_UNVERIFIED';
    case AccountStatus.ACCOUNT_STATUS_GOOD:
      return 'ACCOUNT_STATUS_GOOD';
    case AccountStatus.ACCOUNT_STATUS_BANNED:
      return 'ACCOUNT_STATUS_BANNED';
    case AccountStatus.ACCOUNT_STATUS_TERMINATED:
      return 'ACCOUNT_STATUS_TERMINATED';
    case AccountStatus.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

/** BillingStatus is where an account stands with the payment processor. */
export enum BillingStatus {
  BILLING_STATUS_UNSPECIFIED = 0,
  BILLING_STATUS_UNPAID = 1,
  BILLING_STATUS_TRIAL = 2,
  BILLING_STATUS_PAID = 3,
  BILLING_STATUS_SUSPENDED = 4,
  UNRECOGNIZED = -1,
}

export function billingStatusFromJSON(object: any): BillingStatus {
  switch (object) {
    case 0:
    case 'BILLING_STATUS_UNSPECIFIED':
      return BillingStatus.BILLING_STATUS_UNSPECIFIED;
    case 1:
    case 'BILLING_STATUS_UNPAID':
      return BillingStatus.BILLING_STATUS_UNPAID;
    case 2:
    case 'BILLING_STATUS_TRIAL':
      return BillingStatus.BILLING_STATUS_TRIAL;
    case 3:
    case 'BILLING_STATUS_PAID':
      return BillingStatus.BILLING_STATUS_PAID;
    case 4:
    case 'BILLING_STATUS_SUSPENDED':
      return BillingStatus.BILLING_STATUS_SUSPENDED;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return BillingStatus.UNRECOGNIZED;
  }
}

export function billingStatusToJSON(object: BillingStatus): string {
  switch (object) {
    case BillingStatus.BILLING_STATUS_UNSPECIFIED:
      return 'BILLING_STATUS_UNSPECIFIED';
    case BillingStatus.BILLING_STATUS_UNPAID:
      return 'BILLING_STATUS_UNPAID';
    case BillingStatus.BILLING_STATUS_TRIAL:
      return 'BILLING_STATUS_TRIAL';
    case BillingStatus.BILLING_STATUS_PAID:
      return 'BILLING_STATUS_PAID';
    case BillingStatus.BILLING_STATUS_SUSPENDED:
      return 'BILLING_STATUS_SUSPENDED';
    case BillingStatus.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

/**
 * InvitationStatus is how an invitation stands: still open, or the answer it
 * got.
 */
export enum InvitationStatus {
  INVITATION_STATUS_UNSPECIFIED = 0,
  INVITATION_STATUS_PENDING = 1,
  INVITATION_STATUS_ACCEPTED = 2,
  INVITATION_STATUS_REJECTED = 3,
  INVITATION_STATUS_CANCELLED = 4,
  UNRECOGNIZED = -1,
}

export function invitationStatusFromJSON(object: any): InvitationStatus {
  switch (object) {
    case 0:
    case 'INVITATION_STATUS_UNSPECIFIED':
      return InvitationStatus.INVITATION_STATUS_UNSPECIFIED;
    case 1:
    case 'INVITATION_STATUS_PENDING':
      return InvitationStatus.INVITATION_STATUS_PENDING;
    case 2:
    case 'INVITATION_STATUS_ACCEPTED':
      return InvitationStatus.INVITATION_STATUS_ACCEPTED;
    case 3:
    case 'INVITATION_STATUS_REJECTED':
      return InvitationStatus.INVITATION_STATUS_REJECTED;
    case 4:
    case 'INVITATION_STATUS_CANCELLED':
      return InvitationStatus.INVITATION_STATUS_CANCELLED;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return InvitationStatus.UNRECOGNIZED;
  }
}

export function invitationStatusToJSON(object: InvitationStatus): string {
  switch (object) {
    case InvitationStatus.INVITATION_STATUS_UNSPECIFIED:
      return 'INVITATION_STATUS_UNSPECIFIED';
    case InvitationStatus.INVITATION_STATUS_PENDING:
      return 'INVITATION_STATUS_PENDING';
    case InvitationStatus.INVITATION_STATUS_ACCEPTED:
      return 'INVITATION_STATUS_ACCEPTED';
    case InvitationStatus.INVITATION_STATUS_REJECTED:
      return 'INVITATION_STATUS_REJECTED';
    case InvitationStatus.INVITATION_STATUS_CANCELLED:
      return 'INVITATION_STATUS_CANCELLED';
    case InvitationStatus.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

/** Agreement is a document a user can accept. */
export enum Agreement {
  AGREEMENT_UNSPECIFIED = 0,
  AGREEMENT_TERMS_OF_SERVICE = 1,
  AGREEMENT_PRIVACY_POLICY = 2,
  UNRECOGNIZED = -1,
}

export function agreementFromJSON(object: any): Agreement {
  switch (object) {
    case 0:
    case 'AGREEMENT_UNSPECIFIED':
      return Agreement.AGREEMENT_UNSPECIFIED;
    case 1:
    case 'AGREEMENT_TERMS_OF_SERVICE':
      return Agreement.AGREEMENT_TERMS_OF_SERVICE;
    case 2:
    case 'AGREEMENT_PRIVACY_POLICY':
      return Agreement.AGREEMENT_PRIVACY_POLICY;
    case -1:
    case 'UNRECOGNIZED':
    default:
      return Agreement.UNRECOGNIZED;
  }
}

export function agreementToJSON(object: Agreement): string {
  switch (object) {
    case Agreement.AGREEMENT_UNSPECIFIED:
      return 'AGREEMENT_UNSPECIFIED';
    case Agreement.AGREEMENT_TERMS_OF_SERVICE:
      return 'AGREEMENT_TERMS_OF_SERVICE';
    case Agreement.AGREEMENT_PRIVACY_POLICY:
      return 'AGREEMENT_PRIVACY_POLICY';
    case Agreement.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED';
  }
}

/**
 * User is somebody who can sign in.
 *
 * The credential columns the Go type carries are deliberately absent here; see
 * the file comment. The timestamps *about* those credentials are not secrets
 * and are present, because "two-factor enabled since" and "password last
 * changed" are things a settings page shows.
 */
export interface User {
  id: string;
  /**
   * username is the handle the user signs in with, unique within the directory.
   * It is the folded spelling -- lower case -- which is what the directory is
   * keyed on and what two users cannot differ by. Compare handles with this.
   */
  username: string;
  emailAddress: string;
  /**
   * first_name and last_name are optional: plenty of applications never ask,
   * and plenty of people do not have a name that splits in two.
   */
  firstName: string;
  lastName: string;
  accountStatus: AccountStatus;
  /** account_status_explanation is why, in prose meant for the user. */
  accountStatusExplanation: string;
  /**
   * service_roles are the roles held outside any account -- operator, support,
   * service administrator. They are not membership roles: a support engineer's
   * role does not put them on an account's roster.
   */
  serviceRoles: string[];
  requiresPasswordChange: boolean;
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
  /**
   * email_address_verified_at is when the address was proven reachable. Absent
   * means it has not been.
   */
  emailAddressVerifiedAt: Date | undefined;
  passwordLastChangedAt: Date | undefined;
  /**
   * two_factor_secret_verified_at is when the user first proved they held the
   * secret. A secret issued and never proven is not a second factor, so this
   * being absent is what a sign-in flow checks.
   */
  twoFactorSecretVerifiedAt: Date | undefined;
  /**
   * The two agreement timestamps are timestamps rather than booleans because
   * the question an operator has is "who has not accepted the version published
   * on the 3rd", which a boolean cannot answer.
   */
  lastAcceptedTermsOfService: Date | undefined;
  lastAcceptedPrivacyPolicy: Date | undefined;
  /**
   * display_name is what the person is shown as. It is free-form and unrelated
   * to username: an accented spelling, a nickname and an emoji are all display
   * names on a user whose handle is "renee", nothing is keyed on it, and a
   * rename of the handle leaves it where it is. It is never empty on a user
   * this service returns.
   *
   * A registration names none and is shown under the spelling it submitted in
   * its own username field, which is the default this column was born as.
   * Moving it afterwards is ProfileUpdateInput.display_name, which travels
   * separately from the username field beside it for the reason above: the two
   * are unrelated, so sending one is not sending the other.
   */
  displayName: string;
}

/** BillingAddress is where an account's invoices go. */
export interface BillingAddress {
  line1: string;
  line2: string;
  city: string;
  state: string;
  postalCode: string;
  country: string;
  phone: string;
}

/**
 * Account is a thing users belong to. It is not the tenant -- see the file
 * comment on scope, and identity's own package documentation on why an account
 * is a row here rather than a directory of its own.
 */
export interface Account {
  id: string;
  name: string;
  /**
   * owner_user_id is who owns it. It moves only through
   * TransferAccountOwnership, never through UpdateAccount.
   *
   * The explicit json_name here and on every other id field in this module's
   * schemas is not decoration. protobuf would derive "ownerUserId", and the Go
   * type this renders carries `json:"ownerUserID"` — so a consumer serving the
   * same account over JSON and over gRPC-JSON would emit two spellings of one
   * field. Pinning the name makes the two descriptions of this type agree,
   * which identity/grpc's conformance test then holds them to, and
   * internal/protoconvention holds all eleven schemas to.
   */
  ownerUserId: string;
  billingStatus: BillingStatus;
  paymentProcessorCustomerId: string;
  /**
   * time_zone is an IANA name. An account whose zone the runtime cannot load is
   * refused rather than defaulted to UTC.
   */
  timeZone: string;
  billingAddress: BillingAddress | undefined;
  /** subscription_plan_id is absent for an account on no plan. */
  subscriptionPlanId?: string | undefined;
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
  lastPaymentProviderSyncedAt: Date | undefined;
}

/** Membership puts a user in an account and says what they may do there. */
export interface Membership {
  id: string;
  belongsToUser: string;
  belongsToAccount: string;
  /**
   * roles are this account's roles, resolved to permissions by the consumer's
   * policy alongside the user's service roles.
   */
  roles: string[];
  /**
   * default_account marks the one account the user lands in. Exactly one of a
   * user's live memberships carries it.
   */
  defaultAccount: boolean;
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
}

/**
 * MembershipWithUser is a roster row: the membership and who holds it, so a
 * member list is one call rather than a list and then N reads.
 */
export interface MembershipWithUser {
  user: User | undefined;
  membership: Membership | undefined;
}

/**
 * Invitation is an offer of membership in an account.
 *
 * It carries no token. The token is what a link holds, it is minted server-side
 * and it is cleared from every invitation this service returns; it appears in
 * this schema only as a request field on the two RPCs that answer one.
 */
export interface Invitation {
  id: string;
  belongsToAccount: string;
  /** from_user is who sent it. */
  fromUser: string;
  /**
   * to_user is who answered it, absent while the invitation is still pending or
   * was answered by somebody with no account here.
   */
  toUser?: string | undefined;
  toEmail: string;
  toName: string;
  status: InvitationStatus;
  /** note is the sender's message, and survives the answer untouched. */
  note: string;
  /** status_note is why the answer went the way it did. */
  statusNote: string;
  /**
   * roles are what the invitation promises. They come from here and never from
   * an acceptance's parameters: what somebody was invited to is what they get.
   */
  roles: string[];
  expiresAt: Date | undefined;
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
}

/**
 * Principal is a user together with the memberships that say what they may do,
 * and the account this request is against -- everything a client needs to
 * render "who am I and what may I do", in one call.
 */
export interface Principal {
  user: User | undefined;
  /**
   * active_account_id is the account this request is against: the one asked
   * for, or the user's default when none was. A principal is only ever produced
   * for an account the user is actually a member of.
   */
  activeAccountId: string;
  memberships: Membership[];
}

/**
 * Registration is what a completed registration produced. All three land in one
 * transaction: a user with no account signs in to nothing, and an account whose
 * owner is not on its roster fails every roster-driven permission check.
 */
export interface Registration {
  user: User | undefined;
  account: Account | undefined;
  membership: Membership | undefined;
}

/**
 * Acceptance is what accepting an invitation produced: the answered invitation
 * and the membership it promised.
 */
export interface Acceptance {
  invitation: Invitation | undefined;
  membership: Membership | undefined;
}

/**
 * UserRegistrationInput is the directory half of a registration. It carries no
 * credential and no status: a registrant has proven nothing yet, and the status
 * they start in is the store's decision rather than the caller's.
 */
export interface UserRegistrationInput {
  username: string;
  emailAddress: string;
  firstName: string;
  lastName: string;
}

/**
 * AccountCreationInput is the first account a registrant owns. The owner is the
 * registrant and is not a field here.
 */
export interface AccountCreationInput {
  name: string;
  timeZone: string;
  billingAddress: BillingAddress | undefined;
}

/**
 * ProfileUpdateInput is what a user may change about themselves. Verifying an
 * address is not among them: moving email_address clears whatever verification
 * the old one had, and proving the new one is a fresh link afterwards.
 *
 * Every field is optional, and the distinction is presence rather than value: a
 * field left off the request leaves that column as it was, and a field sent
 * empty clears it. That is what lets a client rename somebody without resending
 * their email address, and what lets a person remove a last name they no longer
 * want shown. A proto3 string with no presence cannot say both, and an update
 * that read absent as "make it empty" would have wiped every field a client
 * did not think to repeat.
 */
export interface ProfileUpdateInput {
  username?: string | undefined;
  emailAddress?: string | undefined;
  firstName?: string | undefined;
  lastName?: string | undefined;
  /**
   * display_name is the name the person is shown under, and it moves on its
   * own: it is not a spelling of username, so a request carrying one and not
   * the other changes exactly the one it named. Sent empty it clears, and a
   * cleared display name reads back as the handle rather than as a blank --
   * see User.display_name.
   */
  displayName?: string | undefined;
}

/**
 * AccountUpdateInput is what an account holder may change. Neither the billing
 * state nor the owner is here: both are moved by flows that do not hold the
 * rest of the account, and a read-modify-write over them would lose whatever a
 * processor webhook or an ownership transfer did in between.
 *
 * Every field is optional, on the same reading as ProfileUpdateInput: absent is
 * "leave it", present is "make it this", and an empty present value clears the
 * field. A rename that carried only a name would otherwise have reset the time
 * zone to UTC and blanked the billing address, and reported all three as
 * changed.
 */
export interface AccountUpdateInput {
  name?: string | undefined;
  timeZone?: string | undefined;
  billingAddress: BillingAddress | undefined;
}

export interface RegisterRequest {
  user: UserRegistrationInput | undefined;
  account: AccountCreationInput | undefined;
  /**
   * owner_roles are the roles the registrant holds in the account they now own.
   * They are the consumer's role names and are required: a membership with none
   * is a member who may do nothing.
   */
  ownerRoles: string[];
}

export interface RegisterResponse {
  registration: Registration | undefined;
}

export interface UpdateProfileRequest {
  input: ProfileUpdateInput | undefined;
}

export interface UpdateProfileResponse {
  user: User | undefined;
}

export interface UpdateAccountRequest {
  accountId: string;
  input: AccountUpdateInput | undefined;
}

export interface UpdateAccountResponse {
  account: Account | undefined;
}

export interface RecordAgreementRequest {
  /**
   * agreements are stamped with one clock read, so accepting two in one call
   * records one moment rather than two a later comparison could order.
   */
  agreements: Agreement[];
}

export interface RecordAgreementResponse {
  user: User | undefined;
}

export interface InviteRequest {
  accountId: string;
  toEmail: string;
  toName: string;
  /** note is the sender's message to the recipient. */
  note: string;
  /** roles are what the invitation promises. */
  roles: string[];
  /**
   * expires_at is when the link stops working. Absent takes the server's
   * configured default, which is what a client that has no opinion should send.
   * A named one has to be later than now and no further ahead than the server's
   * maximum invitation lifetime, or the request is INVALID_ARGUMENT: the default
   * is a bound and not a suggestion, and a request naming its own expiry is not
   * the way around it.
   */
  expiresAt: Date | undefined;
}

export interface InviteResponse {
  /**
   * invitation is redacted, as everything here is. The token it was minted with
   * reached the recipient through whatever the consumer's AfterInvite hook
   * queued, and is not returned to the sender.
   */
  invitation: Invitation | undefined;
}

export interface AcceptInvitationRequest {
  invitationId: string;
  /**
   * token is what the link carried. It is checked against the invitation the id
   * names, and an expired one is reported as expired rather than as absent, so
   * the recipient can be told to ask for another.
   */
  token: string;
  statusNote: string;
}

export interface AcceptInvitationResponse {
  acceptance: Acceptance | undefined;
}

export interface RejectInvitationRequest {
  invitationId: string;
  /**
   * token is required for the same reason it is on acceptance: a rejection
   * arrives from whoever followed the link, and the link is the token. A status
   * write addressed by id alone would let anybody holding an id answer it.
   */
  token: string;
  statusNote: string;
}

export interface RejectInvitationResponse {
  invitation: Invitation | undefined;
}

export interface CancelInvitationRequest {
  /**
   * No token: the sender never had one. They are looking at what they sent,
   * addressed by id.
   */
  invitationId: string;
  statusNote: string;
}

export interface CancelInvitationResponse {
  invitation: Invitation | undefined;
}

/**
 * CreateAccountRequest opens a second account for the caller, with them as its
 * owner.
 *
 * It reserves "owner_user_id" as well as "scope". The owner is the caller,
 * read from the principal the connection resolved, and a field naming one would
 * be a request to create an account somebody else owns — which is not this RPC
 * and would need an authorizer this one deliberately does not have.
 *
 * The new account is not made default. A second account is somewhere a user may
 * go rather than somewhere they are moved to, so a client that wants it to be
 * the landing place calls SetDefaultAccount after.
 */
export interface CreateAccountRequest {
  name: string;
  ownerRoles: string[];
}

export interface CreateAccountResponse {
  account: Account | undefined;
}

export interface TransferAccountOwnershipRequest {
  accountId: string;
  newOwnerUserId: string;
}

export interface TransferAccountOwnershipResponse {
  account: Account | undefined;
}

export interface SetDefaultAccountRequest {
  /**
   * The user is the caller. Setting somebody else's landing account is not a
   * thing this service does.
   */
  accountId: string;
}

export interface SetDefaultAccountResponse {
  membership: Membership | undefined;
}

export interface SetMembershipRolesRequest {
  accountId: string;
  userId: string;
  /**
   * roles replace rather than merge. A caller adding one reads the membership
   * and sends the union; a merging setter cannot revoke.
   */
  roles: string[];
}

export interface SetMembershipRolesResponse {
  membership: Membership | undefined;
}

export interface RemoveMembershipRequest {
  accountId: string;
  userId: string;
}

export interface RemoveMembershipResponse {
  /** membership is the one that was ended, as it stood before it was. */
  membership: Membership | undefined;
}

export interface ArchiveUserRequest {
  userId: string;
}

export interface ArchiveUserResponse {
  user: User | undefined;
}

export interface ArchiveAccountRequest {
  accountId: string;
}

export interface ArchiveAccountResponse {
  /**
   * account is the one that was hidden, as the archival left it. The
   * memberships it ended are deliberately not here: they are the consumer's
   * hook's, which runs inside the operation's transaction, and a roster of
   * unbounded size does not belong on a response whose caller asked to close an
   * account.
   */
  account: Account | undefined;
}

export interface UpdateUserAccountStatusRequest {
  userId: string;
  status: AccountStatus;
  explanation: string;
}

export interface UpdateUserAccountStatusResponse {
  user: User | undefined;
}

export interface SetUserServiceRolesRequest {
  userId: string;
  /**
   * roles replace rather than merge, for the reason SetMembershipRoles gives.
   * This is the write that grants and withdraws operator access.
   */
  roles: string[];
}

export interface SetUserServiceRolesResponse {
  user: User | undefined;
}

export interface GetPrincipalRequest {
  /**
   * active_account_id names which of the caller's accounts this is about.
   * Absent resolves to their default. An account they hold no live membership
   * in is refused rather than answered.
   */
  activeAccountId?: string | undefined;
}

export interface GetPrincipalResponse {
  principal: Principal | undefined;
}

export interface GetUserRequest {
  userId: string;
}

export interface GetUserResponse {
  user: User | undefined;
}

export interface ListUsersRequest {
  filter: QueryFilter | undefined;
}

export interface ListUsersResponse {
  pagination: Pagination | undefined;
  results: User[];
}

export interface SearchUsersByUsernameRequest {
  /**
   * prefix matches from the start of a username. It is a prefix rather than a
   * substring so the read can use the index.
   */
  prefix: string;
  filter: QueryFilter | undefined;
}

export interface SearchUsersByUsernameResponse {
  pagination: Pagination | undefined;
  results: User[];
}

export interface GetAccountRequest {
  accountId: string;
}

export interface GetAccountResponse {
  account: Account | undefined;
}

export interface ListAccountsRequest {
  filter: QueryFilter | undefined;
}

export interface ListAccountsResponse {
  pagination: Pagination | undefined;
  results: Account[];
}

export interface ListAccountsForUserRequest {
  userId: string;
  filter: QueryFilter | undefined;
}

export interface ListAccountsForUserResponse {
  pagination: Pagination | undefined;
  results: Account[];
}

export interface GetMembershipRequest {
  userId: string;
  accountId: string;
}

export interface GetMembershipResponse {
  membership: Membership | undefined;
}

export interface ListMembershipsForUserRequest {
  userId: string;
}

export interface ListMembershipsForUserResponse {
  /**
   * Unpaged, and deliberately: a user's memberships are the set an
   * authorization check needs whole, and half of them is a wrong answer rather
   * than a first page.
   */
  results: Membership[];
}

export interface ListAccountMembersRequest {
  accountId: string;
  filter: QueryFilter | undefined;
}

export interface ListAccountMembersResponse {
  pagination: Pagination | undefined;
  results: MembershipWithUser[];
}

export interface GetInvitationRequest {
  invitationId: string;
}

export interface GetInvitationResponse {
  invitation: Invitation | undefined;
}

export interface ListInvitationsFromUserRequest {
  /** The sender is the caller: this is "what have I sent". */
  filter: QueryFilter | undefined;
}

export interface ListInvitationsFromUserResponse {
  pagination: Pagination | undefined;
  results: Invitation[];
}

export interface ListInvitationsForEmailAddressRequest {
  /**
   * The address is the caller's own, read off the principal's user. An address
   * a client could name is an oracle for which addresses have been invited
   * where.
   */
  filter: QueryFilter | undefined;
}

export interface ListInvitationsForEmailAddressResponse {
  pagination: Pagination | undefined;
  results: Invitation[];
}

function createBaseUser(): User {
  return {
    id: '',
    username: '',
    emailAddress: '',
    firstName: '',
    lastName: '',
    accountStatus: 0,
    accountStatusExplanation: '',
    serviceRoles: [],
    requiresPasswordChange: false,
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
    emailAddressVerifiedAt: undefined,
    passwordLastChangedAt: undefined,
    twoFactorSecretVerifiedAt: undefined,
    lastAcceptedTermsOfService: undefined,
    lastAcceptedPrivacyPolicy: undefined,
    displayName: '',
  };
}

export const User: MessageFns<User> = {
  encode(message: User, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.username !== '') {
      writer.uint32(18).string(message.username);
    }
    if (message.emailAddress !== '') {
      writer.uint32(26).string(message.emailAddress);
    }
    if (message.firstName !== '') {
      writer.uint32(34).string(message.firstName);
    }
    if (message.lastName !== '') {
      writer.uint32(42).string(message.lastName);
    }
    if (message.accountStatus !== 0) {
      writer.uint32(48).int32(message.accountStatus);
    }
    if (message.accountStatusExplanation !== '') {
      writer.uint32(58).string(message.accountStatusExplanation);
    }
    for (const v of message.serviceRoles) {
      writer.uint32(66).string(v!);
    }
    if (message.requiresPasswordChange !== false) {
      writer.uint32(72).bool(message.requiresPasswordChange);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(82).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(90).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(98).fork()).join();
    }
    if (message.emailAddressVerifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.emailAddressVerifiedAt), writer.uint32(106).fork()).join();
    }
    if (message.passwordLastChangedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.passwordLastChangedAt), writer.uint32(114).fork()).join();
    }
    if (message.twoFactorSecretVerifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.twoFactorSecretVerifiedAt), writer.uint32(122).fork()).join();
    }
    if (message.lastAcceptedTermsOfService !== undefined) {
      Timestamp.encode(toTimestamp(message.lastAcceptedTermsOfService), writer.uint32(130).fork()).join();
    }
    if (message.lastAcceptedPrivacyPolicy !== undefined) {
      Timestamp.encode(toTimestamp(message.lastAcceptedPrivacyPolicy), writer.uint32(138).fork()).join();
    }
    if (message.displayName !== '') {
      writer.uint32(154).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): User {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.firstName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.accountStatus = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.accountStatusExplanation = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.serviceRoles.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.requiresPasswordChange = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.emailAddressVerifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.passwordLastChangedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.twoFactorSecretVerifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.lastAcceptedTermsOfService = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.lastAcceptedPrivacyPolicy = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): User {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      username: isSet(object.username) ? globalThis.String(object.username) : '',
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
      firstName: isSet(object.firstName)
        ? globalThis.String(object.firstName)
        : isSet(object.first_name)
          ? globalThis.String(object.first_name)
          : '',
      lastName: isSet(object.lastName)
        ? globalThis.String(object.lastName)
        : isSet(object.last_name)
          ? globalThis.String(object.last_name)
          : '',
      accountStatus: isSet(object.accountStatus)
        ? accountStatusFromJSON(object.accountStatus)
        : isSet(object.account_status)
          ? accountStatusFromJSON(object.account_status)
          : 0,
      accountStatusExplanation: isSet(object.accountStatusExplanation)
        ? globalThis.String(object.accountStatusExplanation)
        : isSet(object.account_status_explanation)
          ? globalThis.String(object.account_status_explanation)
          : '',
      serviceRoles: globalThis.Array.isArray(object?.serviceRoles)
        ? object.serviceRoles.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.service_roles)
          ? object.service_roles.map((e: any) => globalThis.String(e))
          : [],
      requiresPasswordChange: isSet(object.requiresPasswordChange)
        ? globalThis.Boolean(object.requiresPasswordChange)
        : isSet(object.requires_password_change)
          ? globalThis.Boolean(object.requires_password_change)
          : false,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      emailAddressVerifiedAt: isSet(object.emailAddressVerifiedAt)
        ? fromJsonTimestamp(object.emailAddressVerifiedAt)
        : isSet(object.email_address_verified_at)
          ? fromJsonTimestamp(object.email_address_verified_at)
          : undefined,
      passwordLastChangedAt: isSet(object.passwordLastChangedAt)
        ? fromJsonTimestamp(object.passwordLastChangedAt)
        : isSet(object.password_last_changed_at)
          ? fromJsonTimestamp(object.password_last_changed_at)
          : undefined,
      twoFactorSecretVerifiedAt: isSet(object.twoFactorSecretVerifiedAt)
        ? fromJsonTimestamp(object.twoFactorSecretVerifiedAt)
        : isSet(object.two_factor_secret_verified_at)
          ? fromJsonTimestamp(object.two_factor_secret_verified_at)
          : undefined,
      lastAcceptedTermsOfService: isSet(object.lastAcceptedTermsOfService)
        ? fromJsonTimestamp(object.lastAcceptedTermsOfService)
        : isSet(object.last_accepted_terms_of_service)
          ? fromJsonTimestamp(object.last_accepted_terms_of_service)
          : undefined,
      lastAcceptedPrivacyPolicy: isSet(object.lastAcceptedPrivacyPolicy)
        ? fromJsonTimestamp(object.lastAcceptedPrivacyPolicy)
        : isSet(object.last_accepted_privacy_policy)
          ? fromJsonTimestamp(object.last_accepted_privacy_policy)
          : undefined,
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
          ? globalThis.String(object.display_name)
          : '',
    };
  },

  toJSON(message: User): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.username !== '') {
      obj.username = message.username;
    }
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    if (message.firstName !== '') {
      obj.firstName = message.firstName;
    }
    if (message.lastName !== '') {
      obj.lastName = message.lastName;
    }
    if (message.accountStatus !== 0) {
      obj.accountStatus = accountStatusToJSON(message.accountStatus);
    }
    if (message.accountStatusExplanation !== '') {
      obj.accountStatusExplanation = message.accountStatusExplanation;
    }
    if (message.serviceRoles?.length) {
      obj.serviceRoles = message.serviceRoles;
    }
    if (message.requiresPasswordChange !== false) {
      obj.requiresPasswordChange = message.requiresPasswordChange;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.emailAddressVerifiedAt !== undefined) {
      obj.emailAddressVerifiedAt = message.emailAddressVerifiedAt.toISOString();
    }
    if (message.passwordLastChangedAt !== undefined) {
      obj.passwordLastChangedAt = message.passwordLastChangedAt.toISOString();
    }
    if (message.twoFactorSecretVerifiedAt !== undefined) {
      obj.twoFactorSecretVerifiedAt = message.twoFactorSecretVerifiedAt.toISOString();
    }
    if (message.lastAcceptedTermsOfService !== undefined) {
      obj.lastAcceptedTermsOfService = message.lastAcceptedTermsOfService.toISOString();
    }
    if (message.lastAcceptedPrivacyPolicy !== undefined) {
      obj.lastAcceptedPrivacyPolicy = message.lastAcceptedPrivacyPolicy.toISOString();
    }
    if (message.displayName !== '') {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<User>, I>>(base?: I): User {
    return User.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<User>, I>>(object: I): User {
    const message = createBaseUser();
    message.id = object.id ?? '';
    message.username = object.username ?? '';
    message.emailAddress = object.emailAddress ?? '';
    message.firstName = object.firstName ?? '';
    message.lastName = object.lastName ?? '';
    message.accountStatus = object.accountStatus ?? 0;
    message.accountStatusExplanation = object.accountStatusExplanation ?? '';
    message.serviceRoles = object.serviceRoles?.map((e) => e) || [];
    message.requiresPasswordChange = object.requiresPasswordChange ?? false;
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.emailAddressVerifiedAt = object.emailAddressVerifiedAt ?? undefined;
    message.passwordLastChangedAt = object.passwordLastChangedAt ?? undefined;
    message.twoFactorSecretVerifiedAt = object.twoFactorSecretVerifiedAt ?? undefined;
    message.lastAcceptedTermsOfService = object.lastAcceptedTermsOfService ?? undefined;
    message.lastAcceptedPrivacyPolicy = object.lastAcceptedPrivacyPolicy ?? undefined;
    message.displayName = object.displayName ?? '';
    return message;
  },
};

function createBaseBillingAddress(): BillingAddress {
  return { line1: '', line2: '', city: '', state: '', postalCode: '', country: '', phone: '' };
}

export const BillingAddress: MessageFns<BillingAddress> = {
  encode(message: BillingAddress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.line1 !== '') {
      writer.uint32(10).string(message.line1);
    }
    if (message.line2 !== '') {
      writer.uint32(18).string(message.line2);
    }
    if (message.city !== '') {
      writer.uint32(26).string(message.city);
    }
    if (message.state !== '') {
      writer.uint32(34).string(message.state);
    }
    if (message.postalCode !== '') {
      writer.uint32(42).string(message.postalCode);
    }
    if (message.country !== '') {
      writer.uint32(50).string(message.country);
    }
    if (message.phone !== '') {
      writer.uint32(58).string(message.phone);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BillingAddress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBillingAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.line1 = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.line2 = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.city = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.state = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.postalCode = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.country = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.phone = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BillingAddress {
    return {
      line1: isSet(object.line1) ? globalThis.String(object.line1) : '',
      line2: isSet(object.line2) ? globalThis.String(object.line2) : '',
      city: isSet(object.city) ? globalThis.String(object.city) : '',
      state: isSet(object.state) ? globalThis.String(object.state) : '',
      postalCode: isSet(object.postalCode)
        ? globalThis.String(object.postalCode)
        : isSet(object.postal_code)
          ? globalThis.String(object.postal_code)
          : '',
      country: isSet(object.country) ? globalThis.String(object.country) : '',
      phone: isSet(object.phone) ? globalThis.String(object.phone) : '',
    };
  },

  toJSON(message: BillingAddress): unknown {
    const obj: any = {};
    if (message.line1 !== '') {
      obj.line1 = message.line1;
    }
    if (message.line2 !== '') {
      obj.line2 = message.line2;
    }
    if (message.city !== '') {
      obj.city = message.city;
    }
    if (message.state !== '') {
      obj.state = message.state;
    }
    if (message.postalCode !== '') {
      obj.postalCode = message.postalCode;
    }
    if (message.country !== '') {
      obj.country = message.country;
    }
    if (message.phone !== '') {
      obj.phone = message.phone;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BillingAddress>, I>>(base?: I): BillingAddress {
    return BillingAddress.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BillingAddress>, I>>(object: I): BillingAddress {
    const message = createBaseBillingAddress();
    message.line1 = object.line1 ?? '';
    message.line2 = object.line2 ?? '';
    message.city = object.city ?? '';
    message.state = object.state ?? '';
    message.postalCode = object.postalCode ?? '';
    message.country = object.country ?? '';
    message.phone = object.phone ?? '';
    return message;
  },
};

function createBaseAccount(): Account {
  return {
    id: '',
    name: '',
    ownerUserId: '',
    billingStatus: 0,
    paymentProcessorCustomerId: '',
    timeZone: '',
    billingAddress: undefined,
    subscriptionPlanId: undefined,
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
    lastPaymentProviderSyncedAt: undefined,
  };
}

export const Account: MessageFns<Account> = {
  encode(message: Account, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== '') {
      writer.uint32(18).string(message.name);
    }
    if (message.ownerUserId !== '') {
      writer.uint32(26).string(message.ownerUserId);
    }
    if (message.billingStatus !== 0) {
      writer.uint32(32).int32(message.billingStatus);
    }
    if (message.paymentProcessorCustomerId !== '') {
      writer.uint32(42).string(message.paymentProcessorCustomerId);
    }
    if (message.timeZone !== '') {
      writer.uint32(50).string(message.timeZone);
    }
    if (message.billingAddress !== undefined) {
      BillingAddress.encode(message.billingAddress, writer.uint32(58).fork()).join();
    }
    if (message.subscriptionPlanId !== undefined) {
      writer.uint32(66).string(message.subscriptionPlanId);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(74).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(82).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(90).fork()).join();
    }
    if (message.lastPaymentProviderSyncedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastPaymentProviderSyncedAt), writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Account {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerUserId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.billingStatus = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.paymentProcessorCustomerId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timeZone = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.billingAddress = BillingAddress.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.subscriptionPlanId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.lastPaymentProviderSyncedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Account {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      ownerUserId: isSet(object.ownerUserID)
        ? globalThis.String(object.ownerUserID)
        : isSet(object.owner_user_id)
          ? globalThis.String(object.owner_user_id)
          : '',
      billingStatus: isSet(object.billingStatus)
        ? billingStatusFromJSON(object.billingStatus)
        : isSet(object.billing_status)
          ? billingStatusFromJSON(object.billing_status)
          : 0,
      paymentProcessorCustomerId: isSet(object.paymentProcessorCustomerID)
        ? globalThis.String(object.paymentProcessorCustomerID)
        : isSet(object.payment_processor_customer_id)
          ? globalThis.String(object.payment_processor_customer_id)
          : '',
      timeZone: isSet(object.timeZone)
        ? globalThis.String(object.timeZone)
        : isSet(object.time_zone)
          ? globalThis.String(object.time_zone)
          : '',
      billingAddress: isSet(object.billingAddress)
        ? BillingAddress.fromJSON(object.billingAddress)
        : isSet(object.billing_address)
          ? BillingAddress.fromJSON(object.billing_address)
          : undefined,
      subscriptionPlanId: isSet(object.subscriptionPlanID)
        ? globalThis.String(object.subscriptionPlanID)
        : isSet(object.subscription_plan_id)
          ? globalThis.String(object.subscription_plan_id)
          : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      lastPaymentProviderSyncedAt: isSet(object.lastPaymentProviderSyncedAt)
        ? fromJsonTimestamp(object.lastPaymentProviderSyncedAt)
        : isSet(object.last_payment_provider_synced_at)
          ? fromJsonTimestamp(object.last_payment_provider_synced_at)
          : undefined,
    };
  },

  toJSON(message: Account): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.ownerUserId !== '') {
      obj.ownerUserID = message.ownerUserId;
    }
    if (message.billingStatus !== 0) {
      obj.billingStatus = billingStatusToJSON(message.billingStatus);
    }
    if (message.paymentProcessorCustomerId !== '') {
      obj.paymentProcessorCustomerID = message.paymentProcessorCustomerId;
    }
    if (message.timeZone !== '') {
      obj.timeZone = message.timeZone;
    }
    if (message.billingAddress !== undefined) {
      obj.billingAddress = BillingAddress.toJSON(message.billingAddress);
    }
    if (message.subscriptionPlanId !== undefined) {
      obj.subscriptionPlanID = message.subscriptionPlanId;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.lastPaymentProviderSyncedAt !== undefined) {
      obj.lastPaymentProviderSyncedAt = message.lastPaymentProviderSyncedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Account>, I>>(base?: I): Account {
    return Account.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Account>, I>>(object: I): Account {
    const message = createBaseAccount();
    message.id = object.id ?? '';
    message.name = object.name ?? '';
    message.ownerUserId = object.ownerUserId ?? '';
    message.billingStatus = object.billingStatus ?? 0;
    message.paymentProcessorCustomerId = object.paymentProcessorCustomerId ?? '';
    message.timeZone = object.timeZone ?? '';
    message.billingAddress =
      object.billingAddress !== undefined && object.billingAddress !== null
        ? BillingAddress.fromPartial(object.billingAddress)
        : undefined;
    message.subscriptionPlanId = object.subscriptionPlanId ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.lastPaymentProviderSyncedAt = object.lastPaymentProviderSyncedAt ?? undefined;
    return message;
  },
};

function createBaseMembership(): Membership {
  return {
    id: '',
    belongsToUser: '',
    belongsToAccount: '',
    roles: [],
    defaultAccount: false,
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
  };
}

export const Membership: MessageFns<Membership> = {
  encode(message: Membership, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.belongsToUser !== '') {
      writer.uint32(18).string(message.belongsToUser);
    }
    if (message.belongsToAccount !== '') {
      writer.uint32(26).string(message.belongsToAccount);
    }
    for (const v of message.roles) {
      writer.uint32(34).string(v!);
    }
    if (message.defaultAccount !== false) {
      writer.uint32(40).bool(message.defaultAccount);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(58).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Membership {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMembership();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.belongsToUser = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.belongsToAccount = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.defaultAccount = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Membership {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      belongsToUser: isSet(object.belongsToUser)
        ? globalThis.String(object.belongsToUser)
        : isSet(object.belongs_to_user)
          ? globalThis.String(object.belongs_to_user)
          : '',
      belongsToAccount: isSet(object.belongsToAccount)
        ? globalThis.String(object.belongsToAccount)
        : isSet(object.belongs_to_account)
          ? globalThis.String(object.belongs_to_account)
          : '',
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
      defaultAccount: isSet(object.defaultAccount)
        ? globalThis.Boolean(object.defaultAccount)
        : isSet(object.default_account)
          ? globalThis.Boolean(object.default_account)
          : false,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
    };
  },

  toJSON(message: Membership): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.belongsToUser !== '') {
      obj.belongsToUser = message.belongsToUser;
    }
    if (message.belongsToAccount !== '') {
      obj.belongsToAccount = message.belongsToAccount;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.defaultAccount !== false) {
      obj.defaultAccount = message.defaultAccount;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Membership>, I>>(base?: I): Membership {
    return Membership.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Membership>, I>>(object: I): Membership {
    const message = createBaseMembership();
    message.id = object.id ?? '';
    message.belongsToUser = object.belongsToUser ?? '';
    message.belongsToAccount = object.belongsToAccount ?? '';
    message.roles = object.roles?.map((e) => e) || [];
    message.defaultAccount = object.defaultAccount ?? false;
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    return message;
  },
};

function createBaseMembershipWithUser(): MembershipWithUser {
  return { user: undefined, membership: undefined };
}

export const MembershipWithUser: MessageFns<MembershipWithUser> = {
  encode(message: MembershipWithUser, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MembershipWithUser {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMembershipWithUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MembershipWithUser {
    return {
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined,
    };
  },

  toJSON(message: MembershipWithUser): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MembershipWithUser>, I>>(base?: I): MembershipWithUser {
    return MembershipWithUser.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MembershipWithUser>, I>>(object: I): MembershipWithUser {
    const message = createBaseMembershipWithUser();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseInvitation(): Invitation {
  return {
    id: '',
    belongsToAccount: '',
    fromUser: '',
    toUser: undefined,
    toEmail: '',
    toName: '',
    status: 0,
    note: '',
    statusNote: '',
    roles: [],
    expiresAt: undefined,
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
  };
}

export const Invitation: MessageFns<Invitation> = {
  encode(message: Invitation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.belongsToAccount !== '') {
      writer.uint32(18).string(message.belongsToAccount);
    }
    if (message.fromUser !== '') {
      writer.uint32(26).string(message.fromUser);
    }
    if (message.toUser !== undefined) {
      writer.uint32(34).string(message.toUser);
    }
    if (message.toEmail !== '') {
      writer.uint32(42).string(message.toEmail);
    }
    if (message.toName !== '') {
      writer.uint32(50).string(message.toName);
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.note !== '') {
      writer.uint32(66).string(message.note);
    }
    if (message.statusNote !== '') {
      writer.uint32(74).string(message.statusNote);
    }
    for (const v of message.roles) {
      writer.uint32(82).string(v!);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(90).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(98).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(106).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Invitation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInvitation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.belongsToAccount = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fromUser = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toUser = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.toEmail = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.toName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.note = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.statusNote = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Invitation {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      belongsToAccount: isSet(object.belongsToAccount)
        ? globalThis.String(object.belongsToAccount)
        : isSet(object.belongs_to_account)
          ? globalThis.String(object.belongs_to_account)
          : '',
      fromUser: isSet(object.fromUser)
        ? globalThis.String(object.fromUser)
        : isSet(object.from_user)
          ? globalThis.String(object.from_user)
          : '',
      toUser: isSet(object.toUser)
        ? globalThis.String(object.toUser)
        : isSet(object.to_user)
          ? globalThis.String(object.to_user)
          : undefined,
      toEmail: isSet(object.toEmail)
        ? globalThis.String(object.toEmail)
        : isSet(object.to_email)
          ? globalThis.String(object.to_email)
          : '',
      toName: isSet(object.toName)
        ? globalThis.String(object.toName)
        : isSet(object.to_name)
          ? globalThis.String(object.to_name)
          : '',
      status: isSet(object.status) ? invitationStatusFromJSON(object.status) : 0,
      note: isSet(object.note) ? globalThis.String(object.note) : '',
      statusNote: isSet(object.statusNote)
        ? globalThis.String(object.statusNote)
        : isSet(object.status_note)
          ? globalThis.String(object.status_note)
          : '',
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
          ? fromJsonTimestamp(object.expires_at)
          : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
    };
  },

  toJSON(message: Invitation): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.belongsToAccount !== '') {
      obj.belongsToAccount = message.belongsToAccount;
    }
    if (message.fromUser !== '') {
      obj.fromUser = message.fromUser;
    }
    if (message.toUser !== undefined) {
      obj.toUser = message.toUser;
    }
    if (message.toEmail !== '') {
      obj.toEmail = message.toEmail;
    }
    if (message.toName !== '') {
      obj.toName = message.toName;
    }
    if (message.status !== 0) {
      obj.status = invitationStatusToJSON(message.status);
    }
    if (message.note !== '') {
      obj.note = message.note;
    }
    if (message.statusNote !== '') {
      obj.statusNote = message.statusNote;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Invitation>, I>>(base?: I): Invitation {
    return Invitation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Invitation>, I>>(object: I): Invitation {
    const message = createBaseInvitation();
    message.id = object.id ?? '';
    message.belongsToAccount = object.belongsToAccount ?? '';
    message.fromUser = object.fromUser ?? '';
    message.toUser = object.toUser ?? undefined;
    message.toEmail = object.toEmail ?? '';
    message.toName = object.toName ?? '';
    message.status = object.status ?? 0;
    message.note = object.note ?? '';
    message.statusNote = object.statusNote ?? '';
    message.roles = object.roles?.map((e) => e) || [];
    message.expiresAt = object.expiresAt ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    return message;
  },
};

function createBasePrincipal(): Principal {
  return { user: undefined, activeAccountId: '', memberships: [] };
}

export const Principal: MessageFns<Principal> = {
  encode(message: Principal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    if (message.activeAccountId !== '') {
      writer.uint32(18).string(message.activeAccountId);
    }
    for (const v of message.memberships) {
      Membership.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Principal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrincipal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.activeAccountId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.memberships.push(Membership.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Principal {
    return {
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      activeAccountId: isSet(object.activeAccountID)
        ? globalThis.String(object.activeAccountID)
        : isSet(object.active_account_id)
          ? globalThis.String(object.active_account_id)
          : '',
      memberships: globalThis.Array.isArray(object?.memberships)
        ? object.memberships.map((e: any) => Membership.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Principal): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.activeAccountId !== '') {
      obj.activeAccountID = message.activeAccountId;
    }
    if (message.memberships?.length) {
      obj.memberships = message.memberships.map((e) => Membership.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Principal>, I>>(base?: I): Principal {
    return Principal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Principal>, I>>(object: I): Principal {
    const message = createBasePrincipal();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    message.activeAccountId = object.activeAccountId ?? '';
    message.memberships = object.memberships?.map((e) => Membership.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRegistration(): Registration {
  return { user: undefined, account: undefined, membership: undefined };
}

export const Registration: MessageFns<Registration> = {
  encode(message: Registration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(18).fork()).join();
    }
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Registration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = Account.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Registration {
    return {
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      account: isSet(object.account) ? Account.fromJSON(object.account) : undefined,
      membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined,
    };
  },

  toJSON(message: Registration): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Registration>, I>>(base?: I): Registration {
    return Registration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Registration>, I>>(object: I): Registration {
    const message = createBaseRegistration();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    message.account =
      object.account !== undefined && object.account !== null ? Account.fromPartial(object.account) : undefined;
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseAcceptance(): Acceptance {
  return { invitation: undefined, membership: undefined };
}

export const Acceptance: MessageFns<Acceptance> = {
  encode(message: Acceptance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitation !== undefined) {
      Invitation.encode(message.invitation, writer.uint32(10).fork()).join();
    }
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Acceptance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAcceptance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitation = Invitation.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Acceptance {
    return {
      invitation: isSet(object.invitation) ? Invitation.fromJSON(object.invitation) : undefined,
      membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined,
    };
  },

  toJSON(message: Acceptance): unknown {
    const obj: any = {};
    if (message.invitation !== undefined) {
      obj.invitation = Invitation.toJSON(message.invitation);
    }
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Acceptance>, I>>(base?: I): Acceptance {
    return Acceptance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Acceptance>, I>>(object: I): Acceptance {
    const message = createBaseAcceptance();
    message.invitation =
      object.invitation !== undefined && object.invitation !== null
        ? Invitation.fromPartial(object.invitation)
        : undefined;
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseUserRegistrationInput(): UserRegistrationInput {
  return { username: '', emailAddress: '', firstName: '', lastName: '' };
}

export const UserRegistrationInput: MessageFns<UserRegistrationInput> = {
  encode(message: UserRegistrationInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== '') {
      writer.uint32(10).string(message.username);
    }
    if (message.emailAddress !== '') {
      writer.uint32(18).string(message.emailAddress);
    }
    if (message.firstName !== '') {
      writer.uint32(26).string(message.firstName);
    }
    if (message.lastName !== '') {
      writer.uint32(34).string(message.lastName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserRegistrationInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserRegistrationInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.firstName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserRegistrationInput {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : '',
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : '',
      firstName: isSet(object.firstName)
        ? globalThis.String(object.firstName)
        : isSet(object.first_name)
          ? globalThis.String(object.first_name)
          : '',
      lastName: isSet(object.lastName)
        ? globalThis.String(object.lastName)
        : isSet(object.last_name)
          ? globalThis.String(object.last_name)
          : '',
    };
  },

  toJSON(message: UserRegistrationInput): unknown {
    const obj: any = {};
    if (message.username !== '') {
      obj.username = message.username;
    }
    if (message.emailAddress !== '') {
      obj.emailAddress = message.emailAddress;
    }
    if (message.firstName !== '') {
      obj.firstName = message.firstName;
    }
    if (message.lastName !== '') {
      obj.lastName = message.lastName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserRegistrationInput>, I>>(base?: I): UserRegistrationInput {
    return UserRegistrationInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserRegistrationInput>, I>>(object: I): UserRegistrationInput {
    const message = createBaseUserRegistrationInput();
    message.username = object.username ?? '';
    message.emailAddress = object.emailAddress ?? '';
    message.firstName = object.firstName ?? '';
    message.lastName = object.lastName ?? '';
    return message;
  },
};

function createBaseAccountCreationInput(): AccountCreationInput {
  return { name: '', timeZone: '', billingAddress: undefined };
}

export const AccountCreationInput: MessageFns<AccountCreationInput> = {
  encode(message: AccountCreationInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== '') {
      writer.uint32(10).string(message.name);
    }
    if (message.timeZone !== '') {
      writer.uint32(18).string(message.timeZone);
    }
    if (message.billingAddress !== undefined) {
      BillingAddress.encode(message.billingAddress, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountCreationInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountCreationInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeZone = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.billingAddress = BillingAddress.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountCreationInput {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      timeZone: isSet(object.timeZone)
        ? globalThis.String(object.timeZone)
        : isSet(object.time_zone)
          ? globalThis.String(object.time_zone)
          : '',
      billingAddress: isSet(object.billingAddress)
        ? BillingAddress.fromJSON(object.billingAddress)
        : isSet(object.billing_address)
          ? BillingAddress.fromJSON(object.billing_address)
          : undefined,
    };
  },

  toJSON(message: AccountCreationInput): unknown {
    const obj: any = {};
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.timeZone !== '') {
      obj.timeZone = message.timeZone;
    }
    if (message.billingAddress !== undefined) {
      obj.billingAddress = BillingAddress.toJSON(message.billingAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountCreationInput>, I>>(base?: I): AccountCreationInput {
    return AccountCreationInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountCreationInput>, I>>(object: I): AccountCreationInput {
    const message = createBaseAccountCreationInput();
    message.name = object.name ?? '';
    message.timeZone = object.timeZone ?? '';
    message.billingAddress =
      object.billingAddress !== undefined && object.billingAddress !== null
        ? BillingAddress.fromPartial(object.billingAddress)
        : undefined;
    return message;
  },
};

function createBaseProfileUpdateInput(): ProfileUpdateInput {
  return {
    username: undefined,
    emailAddress: undefined,
    firstName: undefined,
    lastName: undefined,
    displayName: undefined,
  };
}

export const ProfileUpdateInput: MessageFns<ProfileUpdateInput> = {
  encode(message: ProfileUpdateInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== undefined) {
      writer.uint32(10).string(message.username);
    }
    if (message.emailAddress !== undefined) {
      writer.uint32(18).string(message.emailAddress);
    }
    if (message.firstName !== undefined) {
      writer.uint32(26).string(message.firstName);
    }
    if (message.lastName !== undefined) {
      writer.uint32(34).string(message.lastName);
    }
    if (message.displayName !== undefined) {
      writer.uint32(42).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProfileUpdateInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProfileUpdateInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.firstName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProfileUpdateInput {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : undefined,
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
          ? globalThis.String(object.email_address)
          : undefined,
      firstName: isSet(object.firstName)
        ? globalThis.String(object.firstName)
        : isSet(object.first_name)
          ? globalThis.String(object.first_name)
          : undefined,
      lastName: isSet(object.lastName)
        ? globalThis.String(object.lastName)
        : isSet(object.last_name)
          ? globalThis.String(object.last_name)
          : undefined,
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
          ? globalThis.String(object.display_name)
          : undefined,
    };
  },

  toJSON(message: ProfileUpdateInput): unknown {
    const obj: any = {};
    if (message.username !== undefined) {
      obj.username = message.username;
    }
    if (message.emailAddress !== undefined) {
      obj.emailAddress = message.emailAddress;
    }
    if (message.firstName !== undefined) {
      obj.firstName = message.firstName;
    }
    if (message.lastName !== undefined) {
      obj.lastName = message.lastName;
    }
    if (message.displayName !== undefined) {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProfileUpdateInput>, I>>(base?: I): ProfileUpdateInput {
    return ProfileUpdateInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProfileUpdateInput>, I>>(object: I): ProfileUpdateInput {
    const message = createBaseProfileUpdateInput();
    message.username = object.username ?? undefined;
    message.emailAddress = object.emailAddress ?? undefined;
    message.firstName = object.firstName ?? undefined;
    message.lastName = object.lastName ?? undefined;
    message.displayName = object.displayName ?? undefined;
    return message;
  },
};

function createBaseAccountUpdateInput(): AccountUpdateInput {
  return { name: undefined, timeZone: undefined, billingAddress: undefined };
}

export const AccountUpdateInput: MessageFns<AccountUpdateInput> = {
  encode(message: AccountUpdateInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined) {
      writer.uint32(10).string(message.name);
    }
    if (message.timeZone !== undefined) {
      writer.uint32(18).string(message.timeZone);
    }
    if (message.billingAddress !== undefined) {
      BillingAddress.encode(message.billingAddress, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountUpdateInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountUpdateInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeZone = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.billingAddress = BillingAddress.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountUpdateInput {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      timeZone: isSet(object.timeZone)
        ? globalThis.String(object.timeZone)
        : isSet(object.time_zone)
          ? globalThis.String(object.time_zone)
          : undefined,
      billingAddress: isSet(object.billingAddress)
        ? BillingAddress.fromJSON(object.billingAddress)
        : isSet(object.billing_address)
          ? BillingAddress.fromJSON(object.billing_address)
          : undefined,
    };
  },

  toJSON(message: AccountUpdateInput): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.timeZone !== undefined) {
      obj.timeZone = message.timeZone;
    }
    if (message.billingAddress !== undefined) {
      obj.billingAddress = BillingAddress.toJSON(message.billingAddress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AccountUpdateInput>, I>>(base?: I): AccountUpdateInput {
    return AccountUpdateInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AccountUpdateInput>, I>>(object: I): AccountUpdateInput {
    const message = createBaseAccountUpdateInput();
    message.name = object.name ?? undefined;
    message.timeZone = object.timeZone ?? undefined;
    message.billingAddress =
      object.billingAddress !== undefined && object.billingAddress !== null
        ? BillingAddress.fromPartial(object.billingAddress)
        : undefined;
    return message;
  },
};

function createBaseRegisterRequest(): RegisterRequest {
  return { user: undefined, account: undefined, ownerRoles: [] };
}

export const RegisterRequest: MessageFns<RegisterRequest> = {
  encode(message: RegisterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      UserRegistrationInput.encode(message.user, writer.uint32(10).fork()).join();
    }
    if (message.account !== undefined) {
      AccountCreationInput.encode(message.account, writer.uint32(18).fork()).join();
    }
    for (const v of message.ownerRoles) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = UserRegistrationInput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.account = AccountCreationInput.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerRoles.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRequest {
    return {
      user: isSet(object.user) ? UserRegistrationInput.fromJSON(object.user) : undefined,
      account: isSet(object.account) ? AccountCreationInput.fromJSON(object.account) : undefined,
      ownerRoles: globalThis.Array.isArray(object?.ownerRoles)
        ? object.ownerRoles.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.owner_roles)
          ? object.owner_roles.map((e: any) => globalThis.String(e))
          : [],
    };
  },

  toJSON(message: RegisterRequest): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = UserRegistrationInput.toJSON(message.user);
    }
    if (message.account !== undefined) {
      obj.account = AccountCreationInput.toJSON(message.account);
    }
    if (message.ownerRoles?.length) {
      obj.ownerRoles = message.ownerRoles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterRequest>, I>>(base?: I): RegisterRequest {
    return RegisterRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterRequest>, I>>(object: I): RegisterRequest {
    const message = createBaseRegisterRequest();
    message.user =
      object.user !== undefined && object.user !== null ? UserRegistrationInput.fromPartial(object.user) : undefined;
    message.account =
      object.account !== undefined && object.account !== null
        ? AccountCreationInput.fromPartial(object.account)
        : undefined;
    message.ownerRoles = object.ownerRoles?.map((e) => e) || [];
    return message;
  },
};

function createBaseRegisterResponse(): RegisterResponse {
  return { registration: undefined };
}

export const RegisterResponse: MessageFns<RegisterResponse> = {
  encode(message: RegisterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registration !== undefined) {
      Registration.encode(message.registration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registration = Registration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterResponse {
    return { registration: isSet(object.registration) ? Registration.fromJSON(object.registration) : undefined };
  },

  toJSON(message: RegisterResponse): unknown {
    const obj: any = {};
    if (message.registration !== undefined) {
      obj.registration = Registration.toJSON(message.registration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterResponse>, I>>(base?: I): RegisterResponse {
    return RegisterResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterResponse>, I>>(object: I): RegisterResponse {
    const message = createBaseRegisterResponse();
    message.registration =
      object.registration !== undefined && object.registration !== null
        ? Registration.fromPartial(object.registration)
        : undefined;
    return message;
  },
};

function createBaseUpdateProfileRequest(): UpdateProfileRequest {
  return { input: undefined };
}

export const UpdateProfileRequest: MessageFns<UpdateProfileRequest> = {
  encode(message: UpdateProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.input !== undefined) {
      ProfileUpdateInput.encode(message.input, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.input = ProfileUpdateInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateProfileRequest {
    return { input: isSet(object.input) ? ProfileUpdateInput.fromJSON(object.input) : undefined };
  },

  toJSON(message: UpdateProfileRequest): unknown {
    const obj: any = {};
    if (message.input !== undefined) {
      obj.input = ProfileUpdateInput.toJSON(message.input);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateProfileRequest>, I>>(base?: I): UpdateProfileRequest {
    return UpdateProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateProfileRequest>, I>>(object: I): UpdateProfileRequest {
    const message = createBaseUpdateProfileRequest();
    message.input =
      object.input !== undefined && object.input !== null ? ProfileUpdateInput.fromPartial(object.input) : undefined;
    return message;
  },
};

function createBaseUpdateProfileResponse(): UpdateProfileResponse {
  return { user: undefined };
}

export const UpdateProfileResponse: MessageFns<UpdateProfileResponse> = {
  encode(message: UpdateProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateProfileResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: UpdateProfileResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateProfileResponse>, I>>(base?: I): UpdateProfileResponse {
    return UpdateProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateProfileResponse>, I>>(object: I): UpdateProfileResponse {
    const message = createBaseUpdateProfileResponse();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseUpdateAccountRequest(): UpdateAccountRequest {
  return { accountId: '', input: undefined };
}

export const UpdateAccountRequest: MessageFns<UpdateAccountRequest> = {
  encode(message: UpdateAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    if (message.input !== undefined) {
      AccountUpdateInput.encode(message.input, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.input = AccountUpdateInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateAccountRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      input: isSet(object.input) ? AccountUpdateInput.fromJSON(object.input) : undefined,
    };
  },

  toJSON(message: UpdateAccountRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    if (message.input !== undefined) {
      obj.input = AccountUpdateInput.toJSON(message.input);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateAccountRequest>, I>>(base?: I): UpdateAccountRequest {
    return UpdateAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateAccountRequest>, I>>(object: I): UpdateAccountRequest {
    const message = createBaseUpdateAccountRequest();
    message.accountId = object.accountId ?? '';
    message.input =
      object.input !== undefined && object.input !== null ? AccountUpdateInput.fromPartial(object.input) : undefined;
    return message;
  },
};

function createBaseUpdateAccountResponse(): UpdateAccountResponse {
  return { account: undefined };
}

export const UpdateAccountResponse: MessageFns<UpdateAccountResponse> = {
  encode(message: UpdateAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = Account.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateAccountResponse {
    return { account: isSet(object.account) ? Account.fromJSON(object.account) : undefined };
  },

  toJSON(message: UpdateAccountResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateAccountResponse>, I>>(base?: I): UpdateAccountResponse {
    return UpdateAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateAccountResponse>, I>>(object: I): UpdateAccountResponse {
    const message = createBaseUpdateAccountResponse();
    message.account =
      object.account !== undefined && object.account !== null ? Account.fromPartial(object.account) : undefined;
    return message;
  },
};

function createBaseRecordAgreementRequest(): RecordAgreementRequest {
  return { agreements: [] };
}

export const RecordAgreementRequest: MessageFns<RecordAgreementRequest> = {
  encode(message: RecordAgreementRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.agreements) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordAgreementRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordAgreementRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.agreements.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.agreements.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordAgreementRequest {
    return {
      agreements: globalThis.Array.isArray(object?.agreements)
        ? object.agreements.map((e: any) => agreementFromJSON(e))
        : [],
    };
  },

  toJSON(message: RecordAgreementRequest): unknown {
    const obj: any = {};
    if (message.agreements?.length) {
      obj.agreements = message.agreements.map((e) => agreementToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecordAgreementRequest>, I>>(base?: I): RecordAgreementRequest {
    return RecordAgreementRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordAgreementRequest>, I>>(object: I): RecordAgreementRequest {
    const message = createBaseRecordAgreementRequest();
    message.agreements = object.agreements?.map((e) => e) || [];
    return message;
  },
};

function createBaseRecordAgreementResponse(): RecordAgreementResponse {
  return { user: undefined };
}

export const RecordAgreementResponse: MessageFns<RecordAgreementResponse> = {
  encode(message: RecordAgreementResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordAgreementResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordAgreementResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordAgreementResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: RecordAgreementResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecordAgreementResponse>, I>>(base?: I): RecordAgreementResponse {
    return RecordAgreementResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordAgreementResponse>, I>>(object: I): RecordAgreementResponse {
    const message = createBaseRecordAgreementResponse();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseInviteRequest(): InviteRequest {
  return { accountId: '', toEmail: '', toName: '', note: '', roles: [], expiresAt: undefined };
}

export const InviteRequest: MessageFns<InviteRequest> = {
  encode(message: InviteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    if (message.toEmail !== '') {
      writer.uint32(18).string(message.toEmail);
    }
    if (message.toName !== '') {
      writer.uint32(26).string(message.toName);
    }
    if (message.note !== '') {
      writer.uint32(34).string(message.note);
    }
    for (const v of message.roles) {
      writer.uint32(42).string(v!);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InviteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toEmail = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.note = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      toEmail: isSet(object.toEmail)
        ? globalThis.String(object.toEmail)
        : isSet(object.to_email)
          ? globalThis.String(object.to_email)
          : '',
      toName: isSet(object.toName)
        ? globalThis.String(object.toName)
        : isSet(object.to_name)
          ? globalThis.String(object.to_name)
          : '',
      note: isSet(object.note) ? globalThis.String(object.note) : '',
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
          ? fromJsonTimestamp(object.expires_at)
          : undefined,
    };
  },

  toJSON(message: InviteRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    if (message.toEmail !== '') {
      obj.toEmail = message.toEmail;
    }
    if (message.toName !== '') {
      obj.toName = message.toName;
    }
    if (message.note !== '') {
      obj.note = message.note;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteRequest>, I>>(base?: I): InviteRequest {
    return InviteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteRequest>, I>>(object: I): InviteRequest {
    const message = createBaseInviteRequest();
    message.accountId = object.accountId ?? '';
    message.toEmail = object.toEmail ?? '';
    message.toName = object.toName ?? '';
    message.note = object.note ?? '';
    message.roles = object.roles?.map((e) => e) || [];
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

function createBaseInviteResponse(): InviteResponse {
  return { invitation: undefined };
}

export const InviteResponse: MessageFns<InviteResponse> = {
  encode(message: InviteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitation !== undefined) {
      Invitation.encode(message.invitation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InviteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitation = Invitation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteResponse {
    return { invitation: isSet(object.invitation) ? Invitation.fromJSON(object.invitation) : undefined };
  },

  toJSON(message: InviteResponse): unknown {
    const obj: any = {};
    if (message.invitation !== undefined) {
      obj.invitation = Invitation.toJSON(message.invitation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteResponse>, I>>(base?: I): InviteResponse {
    return InviteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteResponse>, I>>(object: I): InviteResponse {
    const message = createBaseInviteResponse();
    message.invitation =
      object.invitation !== undefined && object.invitation !== null
        ? Invitation.fromPartial(object.invitation)
        : undefined;
    return message;
  },
};

function createBaseAcceptInvitationRequest(): AcceptInvitationRequest {
  return { invitationId: '', token: '', statusNote: '' };
}

export const AcceptInvitationRequest: MessageFns<AcceptInvitationRequest> = {
  encode(message: AcceptInvitationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitationId !== '') {
      writer.uint32(10).string(message.invitationId);
    }
    if (message.token !== '') {
      writer.uint32(18).string(message.token);
    }
    if (message.statusNote !== '') {
      writer.uint32(26).string(message.statusNote);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AcceptInvitationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAcceptInvitationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.statusNote = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AcceptInvitationRequest {
    return {
      invitationId: isSet(object.invitationID)
        ? globalThis.String(object.invitationID)
        : isSet(object.invitation_id)
          ? globalThis.String(object.invitation_id)
          : '',
      token: isSet(object.token) ? globalThis.String(object.token) : '',
      statusNote: isSet(object.statusNote)
        ? globalThis.String(object.statusNote)
        : isSet(object.status_note)
          ? globalThis.String(object.status_note)
          : '',
    };
  },

  toJSON(message: AcceptInvitationRequest): unknown {
    const obj: any = {};
    if (message.invitationId !== '') {
      obj.invitationID = message.invitationId;
    }
    if (message.token !== '') {
      obj.token = message.token;
    }
    if (message.statusNote !== '') {
      obj.statusNote = message.statusNote;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AcceptInvitationRequest>, I>>(base?: I): AcceptInvitationRequest {
    return AcceptInvitationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AcceptInvitationRequest>, I>>(object: I): AcceptInvitationRequest {
    const message = createBaseAcceptInvitationRequest();
    message.invitationId = object.invitationId ?? '';
    message.token = object.token ?? '';
    message.statusNote = object.statusNote ?? '';
    return message;
  },
};

function createBaseAcceptInvitationResponse(): AcceptInvitationResponse {
  return { acceptance: undefined };
}

export const AcceptInvitationResponse: MessageFns<AcceptInvitationResponse> = {
  encode(message: AcceptInvitationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acceptance !== undefined) {
      Acceptance.encode(message.acceptance, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AcceptInvitationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAcceptInvitationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.acceptance = Acceptance.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AcceptInvitationResponse {
    return { acceptance: isSet(object.acceptance) ? Acceptance.fromJSON(object.acceptance) : undefined };
  },

  toJSON(message: AcceptInvitationResponse): unknown {
    const obj: any = {};
    if (message.acceptance !== undefined) {
      obj.acceptance = Acceptance.toJSON(message.acceptance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AcceptInvitationResponse>, I>>(base?: I): AcceptInvitationResponse {
    return AcceptInvitationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AcceptInvitationResponse>, I>>(object: I): AcceptInvitationResponse {
    const message = createBaseAcceptInvitationResponse();
    message.acceptance =
      object.acceptance !== undefined && object.acceptance !== null
        ? Acceptance.fromPartial(object.acceptance)
        : undefined;
    return message;
  },
};

function createBaseRejectInvitationRequest(): RejectInvitationRequest {
  return { invitationId: '', token: '', statusNote: '' };
}

export const RejectInvitationRequest: MessageFns<RejectInvitationRequest> = {
  encode(message: RejectInvitationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitationId !== '') {
      writer.uint32(10).string(message.invitationId);
    }
    if (message.token !== '') {
      writer.uint32(18).string(message.token);
    }
    if (message.statusNote !== '') {
      writer.uint32(26).string(message.statusNote);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RejectInvitationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectInvitationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.statusNote = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RejectInvitationRequest {
    return {
      invitationId: isSet(object.invitationID)
        ? globalThis.String(object.invitationID)
        : isSet(object.invitation_id)
          ? globalThis.String(object.invitation_id)
          : '',
      token: isSet(object.token) ? globalThis.String(object.token) : '',
      statusNote: isSet(object.statusNote)
        ? globalThis.String(object.statusNote)
        : isSet(object.status_note)
          ? globalThis.String(object.status_note)
          : '',
    };
  },

  toJSON(message: RejectInvitationRequest): unknown {
    const obj: any = {};
    if (message.invitationId !== '') {
      obj.invitationID = message.invitationId;
    }
    if (message.token !== '') {
      obj.token = message.token;
    }
    if (message.statusNote !== '') {
      obj.statusNote = message.statusNote;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RejectInvitationRequest>, I>>(base?: I): RejectInvitationRequest {
    return RejectInvitationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RejectInvitationRequest>, I>>(object: I): RejectInvitationRequest {
    const message = createBaseRejectInvitationRequest();
    message.invitationId = object.invitationId ?? '';
    message.token = object.token ?? '';
    message.statusNote = object.statusNote ?? '';
    return message;
  },
};

function createBaseRejectInvitationResponse(): RejectInvitationResponse {
  return { invitation: undefined };
}

export const RejectInvitationResponse: MessageFns<RejectInvitationResponse> = {
  encode(message: RejectInvitationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitation !== undefined) {
      Invitation.encode(message.invitation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RejectInvitationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectInvitationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitation = Invitation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RejectInvitationResponse {
    return { invitation: isSet(object.invitation) ? Invitation.fromJSON(object.invitation) : undefined };
  },

  toJSON(message: RejectInvitationResponse): unknown {
    const obj: any = {};
    if (message.invitation !== undefined) {
      obj.invitation = Invitation.toJSON(message.invitation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RejectInvitationResponse>, I>>(base?: I): RejectInvitationResponse {
    return RejectInvitationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RejectInvitationResponse>, I>>(object: I): RejectInvitationResponse {
    const message = createBaseRejectInvitationResponse();
    message.invitation =
      object.invitation !== undefined && object.invitation !== null
        ? Invitation.fromPartial(object.invitation)
        : undefined;
    return message;
  },
};

function createBaseCancelInvitationRequest(): CancelInvitationRequest {
  return { invitationId: '', statusNote: '' };
}

export const CancelInvitationRequest: MessageFns<CancelInvitationRequest> = {
  encode(message: CancelInvitationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitationId !== '') {
      writer.uint32(10).string(message.invitationId);
    }
    if (message.statusNote !== '') {
      writer.uint32(18).string(message.statusNote);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelInvitationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelInvitationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.statusNote = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelInvitationRequest {
    return {
      invitationId: isSet(object.invitationID)
        ? globalThis.String(object.invitationID)
        : isSet(object.invitation_id)
          ? globalThis.String(object.invitation_id)
          : '',
      statusNote: isSet(object.statusNote)
        ? globalThis.String(object.statusNote)
        : isSet(object.status_note)
          ? globalThis.String(object.status_note)
          : '',
    };
  },

  toJSON(message: CancelInvitationRequest): unknown {
    const obj: any = {};
    if (message.invitationId !== '') {
      obj.invitationID = message.invitationId;
    }
    if (message.statusNote !== '') {
      obj.statusNote = message.statusNote;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelInvitationRequest>, I>>(base?: I): CancelInvitationRequest {
    return CancelInvitationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelInvitationRequest>, I>>(object: I): CancelInvitationRequest {
    const message = createBaseCancelInvitationRequest();
    message.invitationId = object.invitationId ?? '';
    message.statusNote = object.statusNote ?? '';
    return message;
  },
};

function createBaseCancelInvitationResponse(): CancelInvitationResponse {
  return { invitation: undefined };
}

export const CancelInvitationResponse: MessageFns<CancelInvitationResponse> = {
  encode(message: CancelInvitationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitation !== undefined) {
      Invitation.encode(message.invitation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelInvitationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelInvitationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitation = Invitation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelInvitationResponse {
    return { invitation: isSet(object.invitation) ? Invitation.fromJSON(object.invitation) : undefined };
  },

  toJSON(message: CancelInvitationResponse): unknown {
    const obj: any = {};
    if (message.invitation !== undefined) {
      obj.invitation = Invitation.toJSON(message.invitation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelInvitationResponse>, I>>(base?: I): CancelInvitationResponse {
    return CancelInvitationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelInvitationResponse>, I>>(object: I): CancelInvitationResponse {
    const message = createBaseCancelInvitationResponse();
    message.invitation =
      object.invitation !== undefined && object.invitation !== null
        ? Invitation.fromPartial(object.invitation)
        : undefined;
    return message;
  },
};

function createBaseCreateAccountRequest(): CreateAccountRequest {
  return { name: '', ownerRoles: [] };
}

export const CreateAccountRequest: MessageFns<CreateAccountRequest> = {
  encode(message: CreateAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== '') {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.ownerRoles) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ownerRoles.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAccountRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      ownerRoles: globalThis.Array.isArray(object?.ownerRoles)
        ? object.ownerRoles.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.owner_roles)
          ? object.owner_roles.map((e: any) => globalThis.String(e))
          : [],
    };
  },

  toJSON(message: CreateAccountRequest): unknown {
    const obj: any = {};
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.ownerRoles?.length) {
      obj.ownerRoles = message.ownerRoles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAccountRequest>, I>>(base?: I): CreateAccountRequest {
    return CreateAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAccountRequest>, I>>(object: I): CreateAccountRequest {
    const message = createBaseCreateAccountRequest();
    message.name = object.name ?? '';
    message.ownerRoles = object.ownerRoles?.map((e) => e) || [];
    return message;
  },
};

function createBaseCreateAccountResponse(): CreateAccountResponse {
  return { account: undefined };
}

export const CreateAccountResponse: MessageFns<CreateAccountResponse> = {
  encode(message: CreateAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = Account.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAccountResponse {
    return { account: isSet(object.account) ? Account.fromJSON(object.account) : undefined };
  },

  toJSON(message: CreateAccountResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAccountResponse>, I>>(base?: I): CreateAccountResponse {
    return CreateAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAccountResponse>, I>>(object: I): CreateAccountResponse {
    const message = createBaseCreateAccountResponse();
    message.account =
      object.account !== undefined && object.account !== null ? Account.fromPartial(object.account) : undefined;
    return message;
  },
};

function createBaseTransferAccountOwnershipRequest(): TransferAccountOwnershipRequest {
  return { accountId: '', newOwnerUserId: '' };
}

export const TransferAccountOwnershipRequest: MessageFns<TransferAccountOwnershipRequest> = {
  encode(message: TransferAccountOwnershipRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    if (message.newOwnerUserId !== '') {
      writer.uint32(18).string(message.newOwnerUserId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransferAccountOwnershipRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferAccountOwnershipRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newOwnerUserId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferAccountOwnershipRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      newOwnerUserId: isSet(object.newOwnerUserID)
        ? globalThis.String(object.newOwnerUserID)
        : isSet(object.new_owner_user_id)
          ? globalThis.String(object.new_owner_user_id)
          : '',
    };
  },

  toJSON(message: TransferAccountOwnershipRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    if (message.newOwnerUserId !== '') {
      obj.newOwnerUserID = message.newOwnerUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferAccountOwnershipRequest>, I>>(base?: I): TransferAccountOwnershipRequest {
    return TransferAccountOwnershipRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferAccountOwnershipRequest>, I>>(
    object: I,
  ): TransferAccountOwnershipRequest {
    const message = createBaseTransferAccountOwnershipRequest();
    message.accountId = object.accountId ?? '';
    message.newOwnerUserId = object.newOwnerUserId ?? '';
    return message;
  },
};

function createBaseTransferAccountOwnershipResponse(): TransferAccountOwnershipResponse {
  return { account: undefined };
}

export const TransferAccountOwnershipResponse: MessageFns<TransferAccountOwnershipResponse> = {
  encode(message: TransferAccountOwnershipResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransferAccountOwnershipResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferAccountOwnershipResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = Account.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferAccountOwnershipResponse {
    return { account: isSet(object.account) ? Account.fromJSON(object.account) : undefined };
  },

  toJSON(message: TransferAccountOwnershipResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferAccountOwnershipResponse>, I>>(
    base?: I,
  ): TransferAccountOwnershipResponse {
    return TransferAccountOwnershipResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferAccountOwnershipResponse>, I>>(
    object: I,
  ): TransferAccountOwnershipResponse {
    const message = createBaseTransferAccountOwnershipResponse();
    message.account =
      object.account !== undefined && object.account !== null ? Account.fromPartial(object.account) : undefined;
    return message;
  },
};

function createBaseSetDefaultAccountRequest(): SetDefaultAccountRequest {
  return { accountId: '' };
}

export const SetDefaultAccountRequest: MessageFns<SetDefaultAccountRequest> = {
  encode(message: SetDefaultAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetDefaultAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetDefaultAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetDefaultAccountRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
    };
  },

  toJSON(message: SetDefaultAccountRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetDefaultAccountRequest>, I>>(base?: I): SetDefaultAccountRequest {
    return SetDefaultAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetDefaultAccountRequest>, I>>(object: I): SetDefaultAccountRequest {
    const message = createBaseSetDefaultAccountRequest();
    message.accountId = object.accountId ?? '';
    return message;
  },
};

function createBaseSetDefaultAccountResponse(): SetDefaultAccountResponse {
  return { membership: undefined };
}

export const SetDefaultAccountResponse: MessageFns<SetDefaultAccountResponse> = {
  encode(message: SetDefaultAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetDefaultAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetDefaultAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetDefaultAccountResponse {
    return { membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined };
  },

  toJSON(message: SetDefaultAccountResponse): unknown {
    const obj: any = {};
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetDefaultAccountResponse>, I>>(base?: I): SetDefaultAccountResponse {
    return SetDefaultAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetDefaultAccountResponse>, I>>(object: I): SetDefaultAccountResponse {
    const message = createBaseSetDefaultAccountResponse();
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseSetMembershipRolesRequest(): SetMembershipRolesRequest {
  return { accountId: '', userId: '', roles: [] };
}

export const SetMembershipRolesRequest: MessageFns<SetMembershipRolesRequest> = {
  encode(message: SetMembershipRolesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    for (const v of message.roles) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetMembershipRolesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMembershipRolesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetMembershipRolesRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: SetMembershipRolesRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetMembershipRolesRequest>, I>>(base?: I): SetMembershipRolesRequest {
    return SetMembershipRolesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetMembershipRolesRequest>, I>>(object: I): SetMembershipRolesRequest {
    const message = createBaseSetMembershipRolesRequest();
    message.accountId = object.accountId ?? '';
    message.userId = object.userId ?? '';
    message.roles = object.roles?.map((e) => e) || [];
    return message;
  },
};

function createBaseSetMembershipRolesResponse(): SetMembershipRolesResponse {
  return { membership: undefined };
}

export const SetMembershipRolesResponse: MessageFns<SetMembershipRolesResponse> = {
  encode(message: SetMembershipRolesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetMembershipRolesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMembershipRolesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetMembershipRolesResponse {
    return { membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined };
  },

  toJSON(message: SetMembershipRolesResponse): unknown {
    const obj: any = {};
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetMembershipRolesResponse>, I>>(base?: I): SetMembershipRolesResponse {
    return SetMembershipRolesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetMembershipRolesResponse>, I>>(object: I): SetMembershipRolesResponse {
    const message = createBaseSetMembershipRolesResponse();
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseRemoveMembershipRequest(): RemoveMembershipRequest {
  return { accountId: '', userId: '' };
}

export const RemoveMembershipRequest: MessageFns<RemoveMembershipRequest> = {
  encode(message: RemoveMembershipRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    if (message.userId !== '') {
      writer.uint32(18).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveMembershipRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveMembershipRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveMembershipRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
    };
  },

  toJSON(message: RemoveMembershipRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveMembershipRequest>, I>>(base?: I): RemoveMembershipRequest {
    return RemoveMembershipRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveMembershipRequest>, I>>(object: I): RemoveMembershipRequest {
    const message = createBaseRemoveMembershipRequest();
    message.accountId = object.accountId ?? '';
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseRemoveMembershipResponse(): RemoveMembershipResponse {
  return { membership: undefined };
}

export const RemoveMembershipResponse: MessageFns<RemoveMembershipResponse> = {
  encode(message: RemoveMembershipResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveMembershipResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveMembershipResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveMembershipResponse {
    return { membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined };
  },

  toJSON(message: RemoveMembershipResponse): unknown {
    const obj: any = {};
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveMembershipResponse>, I>>(base?: I): RemoveMembershipResponse {
    return RemoveMembershipResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveMembershipResponse>, I>>(object: I): RemoveMembershipResponse {
    const message = createBaseRemoveMembershipResponse();
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseArchiveUserRequest(): ArchiveUserRequest {
  return { userId: '' };
}

export const ArchiveUserRequest: MessageFns<ArchiveUserRequest> = {
  encode(message: ArchiveUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveUserRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
    };
  },

  toJSON(message: ArchiveUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveUserRequest>, I>>(base?: I): ArchiveUserRequest {
    return ArchiveUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveUserRequest>, I>>(object: I): ArchiveUserRequest {
    const message = createBaseArchiveUserRequest();
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseArchiveUserResponse(): ArchiveUserResponse {
  return { user: undefined };
}

export const ArchiveUserResponse: MessageFns<ArchiveUserResponse> = {
  encode(message: ArchiveUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveUserResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: ArchiveUserResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveUserResponse>, I>>(base?: I): ArchiveUserResponse {
    return ArchiveUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveUserResponse>, I>>(object: I): ArchiveUserResponse {
    const message = createBaseArchiveUserResponse();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseArchiveAccountRequest(): ArchiveAccountRequest {
  return { accountId: '' };
}

export const ArchiveAccountRequest: MessageFns<ArchiveAccountRequest> = {
  encode(message: ArchiveAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveAccountRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
    };
  },

  toJSON(message: ArchiveAccountRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveAccountRequest>, I>>(base?: I): ArchiveAccountRequest {
    return ArchiveAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveAccountRequest>, I>>(object: I): ArchiveAccountRequest {
    const message = createBaseArchiveAccountRequest();
    message.accountId = object.accountId ?? '';
    return message;
  },
};

function createBaseArchiveAccountResponse(): ArchiveAccountResponse {
  return { account: undefined };
}

export const ArchiveAccountResponse: MessageFns<ArchiveAccountResponse> = {
  encode(message: ArchiveAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = Account.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveAccountResponse {
    return { account: isSet(object.account) ? Account.fromJSON(object.account) : undefined };
  },

  toJSON(message: ArchiveAccountResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveAccountResponse>, I>>(base?: I): ArchiveAccountResponse {
    return ArchiveAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveAccountResponse>, I>>(object: I): ArchiveAccountResponse {
    const message = createBaseArchiveAccountResponse();
    message.account =
      object.account !== undefined && object.account !== null ? Account.fromPartial(object.account) : undefined;
    return message;
  },
};

function createBaseUpdateUserAccountStatusRequest(): UpdateUserAccountStatusRequest {
  return { userId: '', status: 0, explanation: '' };
}

export const UpdateUserAccountStatusRequest: MessageFns<UpdateUserAccountStatusRequest> = {
  encode(message: UpdateUserAccountStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.explanation !== '') {
      writer.uint32(26).string(message.explanation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateUserAccountStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateUserAccountStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.explanation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateUserAccountStatusRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      status: isSet(object.status) ? accountStatusFromJSON(object.status) : 0,
      explanation: isSet(object.explanation) ? globalThis.String(object.explanation) : '',
    };
  },

  toJSON(message: UpdateUserAccountStatusRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    if (message.status !== 0) {
      obj.status = accountStatusToJSON(message.status);
    }
    if (message.explanation !== '') {
      obj.explanation = message.explanation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateUserAccountStatusRequest>, I>>(base?: I): UpdateUserAccountStatusRequest {
    return UpdateUserAccountStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateUserAccountStatusRequest>, I>>(
    object: I,
  ): UpdateUserAccountStatusRequest {
    const message = createBaseUpdateUserAccountStatusRequest();
    message.userId = object.userId ?? '';
    message.status = object.status ?? 0;
    message.explanation = object.explanation ?? '';
    return message;
  },
};

function createBaseUpdateUserAccountStatusResponse(): UpdateUserAccountStatusResponse {
  return { user: undefined };
}

export const UpdateUserAccountStatusResponse: MessageFns<UpdateUserAccountStatusResponse> = {
  encode(message: UpdateUserAccountStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateUserAccountStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateUserAccountStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateUserAccountStatusResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: UpdateUserAccountStatusResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateUserAccountStatusResponse>, I>>(base?: I): UpdateUserAccountStatusResponse {
    return UpdateUserAccountStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateUserAccountStatusResponse>, I>>(
    object: I,
  ): UpdateUserAccountStatusResponse {
    const message = createBaseUpdateUserAccountStatusResponse();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseSetUserServiceRolesRequest(): SetUserServiceRolesRequest {
  return { userId: '', roles: [] };
}

export const SetUserServiceRolesRequest: MessageFns<SetUserServiceRolesRequest> = {
  encode(message: SetUserServiceRolesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    for (const v of message.roles) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetUserServiceRolesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetUserServiceRolesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetUserServiceRolesRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: SetUserServiceRolesRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetUserServiceRolesRequest>, I>>(base?: I): SetUserServiceRolesRequest {
    return SetUserServiceRolesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetUserServiceRolesRequest>, I>>(object: I): SetUserServiceRolesRequest {
    const message = createBaseSetUserServiceRolesRequest();
    message.userId = object.userId ?? '';
    message.roles = object.roles?.map((e) => e) || [];
    return message;
  },
};

function createBaseSetUserServiceRolesResponse(): SetUserServiceRolesResponse {
  return { user: undefined };
}

export const SetUserServiceRolesResponse: MessageFns<SetUserServiceRolesResponse> = {
  encode(message: SetUserServiceRolesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetUserServiceRolesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetUserServiceRolesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetUserServiceRolesResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: SetUserServiceRolesResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetUserServiceRolesResponse>, I>>(base?: I): SetUserServiceRolesResponse {
    return SetUserServiceRolesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetUserServiceRolesResponse>, I>>(object: I): SetUserServiceRolesResponse {
    const message = createBaseSetUserServiceRolesResponse();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseGetPrincipalRequest(): GetPrincipalRequest {
  return { activeAccountId: undefined };
}

export const GetPrincipalRequest: MessageFns<GetPrincipalRequest> = {
  encode(message: GetPrincipalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activeAccountId !== undefined) {
      writer.uint32(10).string(message.activeAccountId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPrincipalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPrincipalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activeAccountId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPrincipalRequest {
    return {
      activeAccountId: isSet(object.activeAccountID)
        ? globalThis.String(object.activeAccountID)
        : isSet(object.active_account_id)
          ? globalThis.String(object.active_account_id)
          : undefined,
    };
  },

  toJSON(message: GetPrincipalRequest): unknown {
    const obj: any = {};
    if (message.activeAccountId !== undefined) {
      obj.activeAccountID = message.activeAccountId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPrincipalRequest>, I>>(base?: I): GetPrincipalRequest {
    return GetPrincipalRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPrincipalRequest>, I>>(object: I): GetPrincipalRequest {
    const message = createBaseGetPrincipalRequest();
    message.activeAccountId = object.activeAccountId ?? undefined;
    return message;
  },
};

function createBaseGetPrincipalResponse(): GetPrincipalResponse {
  return { principal: undefined };
}

export const GetPrincipalResponse: MessageFns<GetPrincipalResponse> = {
  encode(message: GetPrincipalResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.principal !== undefined) {
      Principal.encode(message.principal, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPrincipalResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPrincipalResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.principal = Principal.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPrincipalResponse {
    return { principal: isSet(object.principal) ? Principal.fromJSON(object.principal) : undefined };
  },

  toJSON(message: GetPrincipalResponse): unknown {
    const obj: any = {};
    if (message.principal !== undefined) {
      obj.principal = Principal.toJSON(message.principal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPrincipalResponse>, I>>(base?: I): GetPrincipalResponse {
    return GetPrincipalResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPrincipalResponse>, I>>(object: I): GetPrincipalResponse {
    const message = createBaseGetPrincipalResponse();
    message.principal =
      object.principal !== undefined && object.principal !== null ? Principal.fromPartial(object.principal) : undefined;
    return message;
  },
};

function createBaseGetUserRequest(): GetUserRequest {
  return { userId: '' };
}

export const GetUserRequest: MessageFns<GetUserRequest> = {
  encode(message: GetUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
    };
  },

  toJSON(message: GetUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserRequest>, I>>(base?: I): GetUserRequest {
    return GetUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserRequest>, I>>(object: I): GetUserRequest {
    const message = createBaseGetUserRequest();
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseGetUserResponse(): GetUserResponse {
  return { user: undefined };
}

export const GetUserResponse: MessageFns<GetUserResponse> = {
  encode(message: GetUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: GetUserResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserResponse>, I>>(base?: I): GetUserResponse {
    return GetUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserResponse>, I>>(object: I): GetUserResponse {
    const message = createBaseGetUserResponse();
    message.user = object.user !== undefined && object.user !== null ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseListUsersRequest(): ListUsersRequest {
  return { filter: undefined };
}

export const ListUsersRequest: MessageFns<ListUsersRequest> = {
  encode(message: ListUsersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListUsersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListUsersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListUsersRequest {
    return { filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined };
  },

  toJSON(message: ListUsersRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListUsersRequest>, I>>(base?: I): ListUsersRequest {
    return ListUsersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListUsersRequest>, I>>(object: I): ListUsersRequest {
    const message = createBaseListUsersRequest();
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListUsersResponse(): ListUsersResponse {
  return { pagination: undefined, results: [] };
}

export const ListUsersResponse: MessageFns<ListUsersResponse> = {
  encode(message: ListUsersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      User.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListUsersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListUsersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(User.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListUsersResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => User.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListUsersResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => User.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListUsersResponse>, I>>(base?: I): ListUsersResponse {
    return ListUsersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListUsersResponse>, I>>(object: I): ListUsersResponse {
    const message = createBaseListUsersResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => User.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSearchUsersByUsernameRequest(): SearchUsersByUsernameRequest {
  return { prefix: '', filter: undefined };
}

export const SearchUsersByUsernameRequest: MessageFns<SearchUsersByUsernameRequest> = {
  encode(message: SearchUsersByUsernameRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== '') {
      writer.uint32(10).string(message.prefix);
    }
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchUsersByUsernameRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchUsersByUsernameRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchUsersByUsernameRequest {
    return {
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : '',
      filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: SearchUsersByUsernameRequest): unknown {
    const obj: any = {};
    if (message.prefix !== '') {
      obj.prefix = message.prefix;
    }
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchUsersByUsernameRequest>, I>>(base?: I): SearchUsersByUsernameRequest {
    return SearchUsersByUsernameRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchUsersByUsernameRequest>, I>>(object: I): SearchUsersByUsernameRequest {
    const message = createBaseSearchUsersByUsernameRequest();
    message.prefix = object.prefix ?? '';
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseSearchUsersByUsernameResponse(): SearchUsersByUsernameResponse {
  return { pagination: undefined, results: [] };
}

export const SearchUsersByUsernameResponse: MessageFns<SearchUsersByUsernameResponse> = {
  encode(message: SearchUsersByUsernameResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      User.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchUsersByUsernameResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchUsersByUsernameResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(User.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchUsersByUsernameResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => User.fromJSON(e)) : [],
    };
  },

  toJSON(message: SearchUsersByUsernameResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => User.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchUsersByUsernameResponse>, I>>(base?: I): SearchUsersByUsernameResponse {
    return SearchUsersByUsernameResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchUsersByUsernameResponse>, I>>(
    object: I,
  ): SearchUsersByUsernameResponse {
    const message = createBaseSearchUsersByUsernameResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => User.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetAccountRequest(): GetAccountRequest {
  return { accountId: '' };
}

export const GetAccountRequest: MessageFns<GetAccountRequest> = {
  encode(message: GetAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAccountRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
    };
  },

  toJSON(message: GetAccountRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAccountRequest>, I>>(base?: I): GetAccountRequest {
    return GetAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAccountRequest>, I>>(object: I): GetAccountRequest {
    const message = createBaseGetAccountRequest();
    message.accountId = object.accountId ?? '';
    return message;
  },
};

function createBaseGetAccountResponse(): GetAccountResponse {
  return { account: undefined };
}

export const GetAccountResponse: MessageFns<GetAccountResponse> = {
  encode(message: GetAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      Account.encode(message.account, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.account = Account.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAccountResponse {
    return { account: isSet(object.account) ? Account.fromJSON(object.account) : undefined };
  },

  toJSON(message: GetAccountResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = Account.toJSON(message.account);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAccountResponse>, I>>(base?: I): GetAccountResponse {
    return GetAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAccountResponse>, I>>(object: I): GetAccountResponse {
    const message = createBaseGetAccountResponse();
    message.account =
      object.account !== undefined && object.account !== null ? Account.fromPartial(object.account) : undefined;
    return message;
  },
};

function createBaseListAccountsRequest(): ListAccountsRequest {
  return { filter: undefined };
}

export const ListAccountsRequest: MessageFns<ListAccountsRequest> = {
  encode(message: ListAccountsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccountsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccountsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccountsRequest {
    return { filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined };
  },

  toJSON(message: ListAccountsRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccountsRequest>, I>>(base?: I): ListAccountsRequest {
    return ListAccountsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccountsRequest>, I>>(object: I): ListAccountsRequest {
    const message = createBaseListAccountsRequest();
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListAccountsResponse(): ListAccountsResponse {
  return { pagination: undefined, results: [] };
}

export const ListAccountsResponse: MessageFns<ListAccountsResponse> = {
  encode(message: ListAccountsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      Account.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccountsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccountsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(Account.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccountsResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => Account.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListAccountsResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => Account.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccountsResponse>, I>>(base?: I): ListAccountsResponse {
    return ListAccountsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccountsResponse>, I>>(object: I): ListAccountsResponse {
    const message = createBaseListAccountsResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => Account.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListAccountsForUserRequest(): ListAccountsForUserRequest {
  return { userId: '', filter: undefined };
}

export const ListAccountsForUserRequest: MessageFns<ListAccountsForUserRequest> = {
  encode(message: ListAccountsForUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccountsForUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccountsForUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccountsForUserRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: ListAccountsForUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccountsForUserRequest>, I>>(base?: I): ListAccountsForUserRequest {
    return ListAccountsForUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccountsForUserRequest>, I>>(object: I): ListAccountsForUserRequest {
    const message = createBaseListAccountsForUserRequest();
    message.userId = object.userId ?? '';
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListAccountsForUserResponse(): ListAccountsForUserResponse {
  return { pagination: undefined, results: [] };
}

export const ListAccountsForUserResponse: MessageFns<ListAccountsForUserResponse> = {
  encode(message: ListAccountsForUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      Account.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccountsForUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccountsForUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(Account.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccountsForUserResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => Account.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListAccountsForUserResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => Account.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccountsForUserResponse>, I>>(base?: I): ListAccountsForUserResponse {
    return ListAccountsForUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccountsForUserResponse>, I>>(object: I): ListAccountsForUserResponse {
    const message = createBaseListAccountsForUserResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => Account.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetMembershipRequest(): GetMembershipRequest {
  return { userId: '', accountId: '' };
}

export const GetMembershipRequest: MessageFns<GetMembershipRequest> = {
  encode(message: GetMembershipRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    if (message.accountId !== '') {
      writer.uint32(18).string(message.accountId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMembershipRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMembershipRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMembershipRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
    };
  },

  toJSON(message: GetMembershipRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMembershipRequest>, I>>(base?: I): GetMembershipRequest {
    return GetMembershipRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMembershipRequest>, I>>(object: I): GetMembershipRequest {
    const message = createBaseGetMembershipRequest();
    message.userId = object.userId ?? '';
    message.accountId = object.accountId ?? '';
    return message;
  },
};

function createBaseGetMembershipResponse(): GetMembershipResponse {
  return { membership: undefined };
}

export const GetMembershipResponse: MessageFns<GetMembershipResponse> = {
  encode(message: GetMembershipResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.membership !== undefined) {
      Membership.encode(message.membership, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMembershipResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMembershipResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.membership = Membership.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMembershipResponse {
    return { membership: isSet(object.membership) ? Membership.fromJSON(object.membership) : undefined };
  },

  toJSON(message: GetMembershipResponse): unknown {
    const obj: any = {};
    if (message.membership !== undefined) {
      obj.membership = Membership.toJSON(message.membership);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMembershipResponse>, I>>(base?: I): GetMembershipResponse {
    return GetMembershipResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMembershipResponse>, I>>(object: I): GetMembershipResponse {
    const message = createBaseGetMembershipResponse();
    message.membership =
      object.membership !== undefined && object.membership !== null
        ? Membership.fromPartial(object.membership)
        : undefined;
    return message;
  },
};

function createBaseListMembershipsForUserRequest(): ListMembershipsForUserRequest {
  return { userId: '' };
}

export const ListMembershipsForUserRequest: MessageFns<ListMembershipsForUserRequest> = {
  encode(message: ListMembershipsForUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMembershipsForUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMembershipsForUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMembershipsForUserRequest {
    return {
      userId: isSet(object.userID)
        ? globalThis.String(object.userID)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
    };
  },

  toJSON(message: ListMembershipsForUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== '') {
      obj.userID = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListMembershipsForUserRequest>, I>>(base?: I): ListMembershipsForUserRequest {
    return ListMembershipsForUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListMembershipsForUserRequest>, I>>(
    object: I,
  ): ListMembershipsForUserRequest {
    const message = createBaseListMembershipsForUserRequest();
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseListMembershipsForUserResponse(): ListMembershipsForUserResponse {
  return { results: [] };
}

export const ListMembershipsForUserResponse: MessageFns<ListMembershipsForUserResponse> = {
  encode(message: ListMembershipsForUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      Membership.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMembershipsForUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMembershipsForUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(Membership.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMembershipsForUserResponse {
    return {
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => Membership.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListMembershipsForUserResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => Membership.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListMembershipsForUserResponse>, I>>(base?: I): ListMembershipsForUserResponse {
    return ListMembershipsForUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListMembershipsForUserResponse>, I>>(
    object: I,
  ): ListMembershipsForUserResponse {
    const message = createBaseListMembershipsForUserResponse();
    message.results = object.results?.map((e) => Membership.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListAccountMembersRequest(): ListAccountMembersRequest {
  return { accountId: '', filter: undefined };
}

export const ListAccountMembersRequest: MessageFns<ListAccountMembersRequest> = {
  encode(message: ListAccountMembersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== '') {
      writer.uint32(10).string(message.accountId);
    }
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccountMembersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccountMembersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccountMembersRequest {
    return {
      accountId: isSet(object.accountID)
        ? globalThis.String(object.accountID)
        : isSet(object.account_id)
          ? globalThis.String(object.account_id)
          : '',
      filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: ListAccountMembersRequest): unknown {
    const obj: any = {};
    if (message.accountId !== '') {
      obj.accountID = message.accountId;
    }
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccountMembersRequest>, I>>(base?: I): ListAccountMembersRequest {
    return ListAccountMembersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccountMembersRequest>, I>>(object: I): ListAccountMembersRequest {
    const message = createBaseListAccountMembersRequest();
    message.accountId = object.accountId ?? '';
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListAccountMembersResponse(): ListAccountMembersResponse {
  return { pagination: undefined, results: [] };
}

export const ListAccountMembersResponse: MessageFns<ListAccountMembersResponse> = {
  encode(message: ListAccountMembersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      MembershipWithUser.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAccountMembersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAccountMembersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(MembershipWithUser.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAccountMembersResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => MembershipWithUser.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListAccountMembersResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => MembershipWithUser.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAccountMembersResponse>, I>>(base?: I): ListAccountMembersResponse {
    return ListAccountMembersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAccountMembersResponse>, I>>(object: I): ListAccountMembersResponse {
    const message = createBaseListAccountMembersResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => MembershipWithUser.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetInvitationRequest(): GetInvitationRequest {
  return { invitationId: '' };
}

export const GetInvitationRequest: MessageFns<GetInvitationRequest> = {
  encode(message: GetInvitationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitationId !== '') {
      writer.uint32(10).string(message.invitationId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetInvitationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInvitationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitationId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetInvitationRequest {
    return {
      invitationId: isSet(object.invitationID)
        ? globalThis.String(object.invitationID)
        : isSet(object.invitation_id)
          ? globalThis.String(object.invitation_id)
          : '',
    };
  },

  toJSON(message: GetInvitationRequest): unknown {
    const obj: any = {};
    if (message.invitationId !== '') {
      obj.invitationID = message.invitationId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetInvitationRequest>, I>>(base?: I): GetInvitationRequest {
    return GetInvitationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetInvitationRequest>, I>>(object: I): GetInvitationRequest {
    const message = createBaseGetInvitationRequest();
    message.invitationId = object.invitationId ?? '';
    return message;
  },
};

function createBaseGetInvitationResponse(): GetInvitationResponse {
  return { invitation: undefined };
}

export const GetInvitationResponse: MessageFns<GetInvitationResponse> = {
  encode(message: GetInvitationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invitation !== undefined) {
      Invitation.encode(message.invitation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetInvitationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInvitationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invitation = Invitation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetInvitationResponse {
    return { invitation: isSet(object.invitation) ? Invitation.fromJSON(object.invitation) : undefined };
  },

  toJSON(message: GetInvitationResponse): unknown {
    const obj: any = {};
    if (message.invitation !== undefined) {
      obj.invitation = Invitation.toJSON(message.invitation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetInvitationResponse>, I>>(base?: I): GetInvitationResponse {
    return GetInvitationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetInvitationResponse>, I>>(object: I): GetInvitationResponse {
    const message = createBaseGetInvitationResponse();
    message.invitation =
      object.invitation !== undefined && object.invitation !== null
        ? Invitation.fromPartial(object.invitation)
        : undefined;
    return message;
  },
};

function createBaseListInvitationsFromUserRequest(): ListInvitationsFromUserRequest {
  return { filter: undefined };
}

export const ListInvitationsFromUserRequest: MessageFns<ListInvitationsFromUserRequest> = {
  encode(message: ListInvitationsFromUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListInvitationsFromUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInvitationsFromUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInvitationsFromUserRequest {
    return { filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined };
  },

  toJSON(message: ListInvitationsFromUserRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListInvitationsFromUserRequest>, I>>(base?: I): ListInvitationsFromUserRequest {
    return ListInvitationsFromUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListInvitationsFromUserRequest>, I>>(
    object: I,
  ): ListInvitationsFromUserRequest {
    const message = createBaseListInvitationsFromUserRequest();
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListInvitationsFromUserResponse(): ListInvitationsFromUserResponse {
  return { pagination: undefined, results: [] };
}

export const ListInvitationsFromUserResponse: MessageFns<ListInvitationsFromUserResponse> = {
  encode(message: ListInvitationsFromUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      Invitation.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListInvitationsFromUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInvitationsFromUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(Invitation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInvitationsFromUserResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => Invitation.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListInvitationsFromUserResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => Invitation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListInvitationsFromUserResponse>, I>>(base?: I): ListInvitationsFromUserResponse {
    return ListInvitationsFromUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListInvitationsFromUserResponse>, I>>(
    object: I,
  ): ListInvitationsFromUserResponse {
    const message = createBaseListInvitationsFromUserResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => Invitation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListInvitationsForEmailAddressRequest(): ListInvitationsForEmailAddressRequest {
  return { filter: undefined };
}

export const ListInvitationsForEmailAddressRequest: MessageFns<ListInvitationsForEmailAddressRequest> = {
  encode(message: ListInvitationsForEmailAddressRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined) {
      QueryFilter.encode(message.filter, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListInvitationsForEmailAddressRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInvitationsForEmailAddressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = QueryFilter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInvitationsForEmailAddressRequest {
    return { filter: isSet(object.filter) ? QueryFilter.fromJSON(object.filter) : undefined };
  },

  toJSON(message: ListInvitationsForEmailAddressRequest): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = QueryFilter.toJSON(message.filter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListInvitationsForEmailAddressRequest>, I>>(
    base?: I,
  ): ListInvitationsForEmailAddressRequest {
    return ListInvitationsForEmailAddressRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListInvitationsForEmailAddressRequest>, I>>(
    object: I,
  ): ListInvitationsForEmailAddressRequest {
    const message = createBaseListInvitationsForEmailAddressRequest();
    message.filter =
      object.filter !== undefined && object.filter !== null ? QueryFilter.fromPartial(object.filter) : undefined;
    return message;
  },
};

function createBaseListInvitationsForEmailAddressResponse(): ListInvitationsForEmailAddressResponse {
  return { pagination: undefined, results: [] };
}

export const ListInvitationsForEmailAddressResponse: MessageFns<ListInvitationsForEmailAddressResponse> = {
  encode(message: ListInvitationsForEmailAddressResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      Pagination.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    for (const v of message.results) {
      Invitation.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListInvitationsForEmailAddressResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInvitationsForEmailAddressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = Pagination.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.results.push(Invitation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInvitationsForEmailAddressResponse {
    return {
      pagination: isSet(object.pagination) ? Pagination.fromJSON(object.pagination) : undefined,
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => Invitation.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListInvitationsForEmailAddressResponse): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = Pagination.toJSON(message.pagination);
    }
    if (message.results?.length) {
      obj.results = message.results.map((e) => Invitation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListInvitationsForEmailAddressResponse>, I>>(
    base?: I,
  ): ListInvitationsForEmailAddressResponse {
    return ListInvitationsForEmailAddressResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListInvitationsForEmailAddressResponse>, I>>(
    object: I,
  ): ListInvitationsForEmailAddressResponse {
    const message = createBaseListInvitationsForEmailAddressResponse();
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? Pagination.fromPartial(object.pagination)
        : undefined;
    message.results = object.results?.map((e) => Invitation.fromPartial(e)) || [];
    return message;
  },
};

/**
 * IdentityService is the directory on the wire: the four nouns, their
 * lifecycle, and the reads a client needs to render them.
 *
 * Every write here is one call into identity.Service, which means one
 * transaction with the consumer's own hook inside it -- the audit entry, the
 * outbox row, the search stamp. Nothing on this service writes outside that
 * seam.
 *
 * Nothing here decides who may call it. The permissions each method requires
 * are a default fragment the server package ships and the consumer composes
 * into its own policy, and who is calling is resolved from the context by the
 * consumer's authentication interceptor.
 */
export type IdentityServiceService = typeof IdentityServiceService;
export const IdentityServiceService = {
  /** The writes. */
  register: {
    path: '/primandproper.platform.identity.v1.IdentityService/Register' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterRequest): Buffer => Buffer.from(RegisterRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterRequest => RegisterRequest.decode(value),
    responseSerialize: (value: RegisterResponse): Buffer => Buffer.from(RegisterResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterResponse => RegisterResponse.decode(value),
  },
  updateProfile: {
    path: '/primandproper.platform.identity.v1.IdentityService/UpdateProfile' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateProfileRequest): Buffer => Buffer.from(UpdateProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateProfileRequest => UpdateProfileRequest.decode(value),
    responseSerialize: (value: UpdateProfileResponse): Buffer =>
      Buffer.from(UpdateProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateProfileResponse => UpdateProfileResponse.decode(value),
  },
  updateAccount: {
    path: '/primandproper.platform.identity.v1.IdentityService/UpdateAccount' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateAccountRequest): Buffer => Buffer.from(UpdateAccountRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateAccountRequest => UpdateAccountRequest.decode(value),
    responseSerialize: (value: UpdateAccountResponse): Buffer =>
      Buffer.from(UpdateAccountResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateAccountResponse => UpdateAccountResponse.decode(value),
  },
  recordAgreement: {
    path: '/primandproper.platform.identity.v1.IdentityService/RecordAgreement' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RecordAgreementRequest): Buffer =>
      Buffer.from(RecordAgreementRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RecordAgreementRequest => RecordAgreementRequest.decode(value),
    responseSerialize: (value: RecordAgreementResponse): Buffer =>
      Buffer.from(RecordAgreementResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RecordAgreementResponse => RecordAgreementResponse.decode(value),
  },
  invite: {
    path: '/primandproper.platform.identity.v1.IdentityService/Invite' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: InviteRequest): Buffer => Buffer.from(InviteRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): InviteRequest => InviteRequest.decode(value),
    responseSerialize: (value: InviteResponse): Buffer => Buffer.from(InviteResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): InviteResponse => InviteResponse.decode(value),
  },
  acceptInvitation: {
    path: '/primandproper.platform.identity.v1.IdentityService/AcceptInvitation' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AcceptInvitationRequest): Buffer =>
      Buffer.from(AcceptInvitationRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AcceptInvitationRequest => AcceptInvitationRequest.decode(value),
    responseSerialize: (value: AcceptInvitationResponse): Buffer =>
      Buffer.from(AcceptInvitationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AcceptInvitationResponse => AcceptInvitationResponse.decode(value),
  },
  rejectInvitation: {
    path: '/primandproper.platform.identity.v1.IdentityService/RejectInvitation' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RejectInvitationRequest): Buffer =>
      Buffer.from(RejectInvitationRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RejectInvitationRequest => RejectInvitationRequest.decode(value),
    responseSerialize: (value: RejectInvitationResponse): Buffer =>
      Buffer.from(RejectInvitationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RejectInvitationResponse => RejectInvitationResponse.decode(value),
  },
  cancelInvitation: {
    path: '/primandproper.platform.identity.v1.IdentityService/CancelInvitation' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CancelInvitationRequest): Buffer =>
      Buffer.from(CancelInvitationRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CancelInvitationRequest => CancelInvitationRequest.decode(value),
    responseSerialize: (value: CancelInvitationResponse): Buffer =>
      Buffer.from(CancelInvitationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CancelInvitationResponse => CancelInvitationResponse.decode(value),
  },
  createAccount: {
    path: '/primandproper.platform.identity.v1.IdentityService/CreateAccount' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateAccountRequest): Buffer => Buffer.from(CreateAccountRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateAccountRequest => CreateAccountRequest.decode(value),
    responseSerialize: (value: CreateAccountResponse): Buffer =>
      Buffer.from(CreateAccountResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateAccountResponse => CreateAccountResponse.decode(value),
  },
  transferAccountOwnership: {
    path: '/primandproper.platform.identity.v1.IdentityService/TransferAccountOwnership' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: TransferAccountOwnershipRequest): Buffer =>
      Buffer.from(TransferAccountOwnershipRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): TransferAccountOwnershipRequest =>
      TransferAccountOwnershipRequest.decode(value),
    responseSerialize: (value: TransferAccountOwnershipResponse): Buffer =>
      Buffer.from(TransferAccountOwnershipResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): TransferAccountOwnershipResponse =>
      TransferAccountOwnershipResponse.decode(value),
  },
  setDefaultAccount: {
    path: '/primandproper.platform.identity.v1.IdentityService/SetDefaultAccount' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetDefaultAccountRequest): Buffer =>
      Buffer.from(SetDefaultAccountRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetDefaultAccountRequest => SetDefaultAccountRequest.decode(value),
    responseSerialize: (value: SetDefaultAccountResponse): Buffer =>
      Buffer.from(SetDefaultAccountResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetDefaultAccountResponse => SetDefaultAccountResponse.decode(value),
  },
  setMembershipRoles: {
    path: '/primandproper.platform.identity.v1.IdentityService/SetMembershipRoles' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetMembershipRolesRequest): Buffer =>
      Buffer.from(SetMembershipRolesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetMembershipRolesRequest => SetMembershipRolesRequest.decode(value),
    responseSerialize: (value: SetMembershipRolesResponse): Buffer =>
      Buffer.from(SetMembershipRolesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetMembershipRolesResponse => SetMembershipRolesResponse.decode(value),
  },
  removeMembership: {
    path: '/primandproper.platform.identity.v1.IdentityService/RemoveMembership' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RemoveMembershipRequest): Buffer =>
      Buffer.from(RemoveMembershipRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RemoveMembershipRequest => RemoveMembershipRequest.decode(value),
    responseSerialize: (value: RemoveMembershipResponse): Buffer =>
      Buffer.from(RemoveMembershipResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RemoveMembershipResponse => RemoveMembershipResponse.decode(value),
  },
  archiveUser: {
    path: '/primandproper.platform.identity.v1.IdentityService/ArchiveUser' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ArchiveUserRequest): Buffer => Buffer.from(ArchiveUserRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ArchiveUserRequest => ArchiveUserRequest.decode(value),
    responseSerialize: (value: ArchiveUserResponse): Buffer => Buffer.from(ArchiveUserResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ArchiveUserResponse => ArchiveUserResponse.decode(value),
  },
  archiveAccount: {
    path: '/primandproper.platform.identity.v1.IdentityService/ArchiveAccount' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ArchiveAccountRequest): Buffer =>
      Buffer.from(ArchiveAccountRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ArchiveAccountRequest => ArchiveAccountRequest.decode(value),
    responseSerialize: (value: ArchiveAccountResponse): Buffer =>
      Buffer.from(ArchiveAccountResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ArchiveAccountResponse => ArchiveAccountResponse.decode(value),
  },
  updateUserAccountStatus: {
    path: '/primandproper.platform.identity.v1.IdentityService/UpdateUserAccountStatus' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateUserAccountStatusRequest): Buffer =>
      Buffer.from(UpdateUserAccountStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateUserAccountStatusRequest => UpdateUserAccountStatusRequest.decode(value),
    responseSerialize: (value: UpdateUserAccountStatusResponse): Buffer =>
      Buffer.from(UpdateUserAccountStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateUserAccountStatusResponse =>
      UpdateUserAccountStatusResponse.decode(value),
  },
  setUserServiceRoles: {
    path: '/primandproper.platform.identity.v1.IdentityService/SetUserServiceRoles' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetUserServiceRolesRequest): Buffer =>
      Buffer.from(SetUserServiceRolesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetUserServiceRolesRequest => SetUserServiceRolesRequest.decode(value),
    responseSerialize: (value: SetUserServiceRolesResponse): Buffer =>
      Buffer.from(SetUserServiceRolesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetUserServiceRolesResponse => SetUserServiceRolesResponse.decode(value),
  },
  /** The reads. */
  getPrincipal: {
    path: '/primandproper.platform.identity.v1.IdentityService/GetPrincipal' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetPrincipalRequest): Buffer => Buffer.from(GetPrincipalRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetPrincipalRequest => GetPrincipalRequest.decode(value),
    responseSerialize: (value: GetPrincipalResponse): Buffer =>
      Buffer.from(GetPrincipalResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetPrincipalResponse => GetPrincipalResponse.decode(value),
  },
  getUser: {
    path: '/primandproper.platform.identity.v1.IdentityService/GetUser' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetUserRequest): Buffer => Buffer.from(GetUserRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetUserRequest => GetUserRequest.decode(value),
    responseSerialize: (value: GetUserResponse): Buffer => Buffer.from(GetUserResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetUserResponse => GetUserResponse.decode(value),
  },
  listUsers: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListUsers' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListUsersRequest): Buffer => Buffer.from(ListUsersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListUsersRequest => ListUsersRequest.decode(value),
    responseSerialize: (value: ListUsersResponse): Buffer => Buffer.from(ListUsersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListUsersResponse => ListUsersResponse.decode(value),
  },
  searchUsersByUsername: {
    path: '/primandproper.platform.identity.v1.IdentityService/SearchUsersByUsername' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SearchUsersByUsernameRequest): Buffer =>
      Buffer.from(SearchUsersByUsernameRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SearchUsersByUsernameRequest => SearchUsersByUsernameRequest.decode(value),
    responseSerialize: (value: SearchUsersByUsernameResponse): Buffer =>
      Buffer.from(SearchUsersByUsernameResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SearchUsersByUsernameResponse => SearchUsersByUsernameResponse.decode(value),
  },
  getAccount: {
    path: '/primandproper.platform.identity.v1.IdentityService/GetAccount' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetAccountRequest): Buffer => Buffer.from(GetAccountRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetAccountRequest => GetAccountRequest.decode(value),
    responseSerialize: (value: GetAccountResponse): Buffer => Buffer.from(GetAccountResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetAccountResponse => GetAccountResponse.decode(value),
  },
  listAccounts: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListAccounts' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListAccountsRequest): Buffer => Buffer.from(ListAccountsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListAccountsRequest => ListAccountsRequest.decode(value),
    responseSerialize: (value: ListAccountsResponse): Buffer =>
      Buffer.from(ListAccountsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListAccountsResponse => ListAccountsResponse.decode(value),
  },
  listAccountsForUser: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListAccountsForUser' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListAccountsForUserRequest): Buffer =>
      Buffer.from(ListAccountsForUserRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListAccountsForUserRequest => ListAccountsForUserRequest.decode(value),
    responseSerialize: (value: ListAccountsForUserResponse): Buffer =>
      Buffer.from(ListAccountsForUserResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListAccountsForUserResponse => ListAccountsForUserResponse.decode(value),
  },
  getMembership: {
    path: '/primandproper.platform.identity.v1.IdentityService/GetMembership' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetMembershipRequest): Buffer => Buffer.from(GetMembershipRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetMembershipRequest => GetMembershipRequest.decode(value),
    responseSerialize: (value: GetMembershipResponse): Buffer =>
      Buffer.from(GetMembershipResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetMembershipResponse => GetMembershipResponse.decode(value),
  },
  listMembershipsForUser: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListMembershipsForUser' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListMembershipsForUserRequest): Buffer =>
      Buffer.from(ListMembershipsForUserRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListMembershipsForUserRequest => ListMembershipsForUserRequest.decode(value),
    responseSerialize: (value: ListMembershipsForUserResponse): Buffer =>
      Buffer.from(ListMembershipsForUserResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListMembershipsForUserResponse =>
      ListMembershipsForUserResponse.decode(value),
  },
  listAccountMembers: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListAccountMembers' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListAccountMembersRequest): Buffer =>
      Buffer.from(ListAccountMembersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListAccountMembersRequest => ListAccountMembersRequest.decode(value),
    responseSerialize: (value: ListAccountMembersResponse): Buffer =>
      Buffer.from(ListAccountMembersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListAccountMembersResponse => ListAccountMembersResponse.decode(value),
  },
  getInvitation: {
    path: '/primandproper.platform.identity.v1.IdentityService/GetInvitation' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetInvitationRequest): Buffer => Buffer.from(GetInvitationRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetInvitationRequest => GetInvitationRequest.decode(value),
    responseSerialize: (value: GetInvitationResponse): Buffer =>
      Buffer.from(GetInvitationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetInvitationResponse => GetInvitationResponse.decode(value),
  },
  listInvitationsFromUser: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListInvitationsFromUser' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListInvitationsFromUserRequest): Buffer =>
      Buffer.from(ListInvitationsFromUserRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListInvitationsFromUserRequest => ListInvitationsFromUserRequest.decode(value),
    responseSerialize: (value: ListInvitationsFromUserResponse): Buffer =>
      Buffer.from(ListInvitationsFromUserResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListInvitationsFromUserResponse =>
      ListInvitationsFromUserResponse.decode(value),
  },
  listInvitationsForEmailAddress: {
    path: '/primandproper.platform.identity.v1.IdentityService/ListInvitationsForEmailAddress' as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListInvitationsForEmailAddressRequest): Buffer =>
      Buffer.from(ListInvitationsForEmailAddressRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListInvitationsForEmailAddressRequest =>
      ListInvitationsForEmailAddressRequest.decode(value),
    responseSerialize: (value: ListInvitationsForEmailAddressResponse): Buffer =>
      Buffer.from(ListInvitationsForEmailAddressResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListInvitationsForEmailAddressResponse =>
      ListInvitationsForEmailAddressResponse.decode(value),
  },
} as const;

export interface IdentityServiceServer extends UntypedServiceImplementation {
  /** The writes. */
  register: handleUnaryCall<RegisterRequest, RegisterResponse>;
  updateProfile: handleUnaryCall<UpdateProfileRequest, UpdateProfileResponse>;
  updateAccount: handleUnaryCall<UpdateAccountRequest, UpdateAccountResponse>;
  recordAgreement: handleUnaryCall<RecordAgreementRequest, RecordAgreementResponse>;
  invite: handleUnaryCall<InviteRequest, InviteResponse>;
  acceptInvitation: handleUnaryCall<AcceptInvitationRequest, AcceptInvitationResponse>;
  rejectInvitation: handleUnaryCall<RejectInvitationRequest, RejectInvitationResponse>;
  cancelInvitation: handleUnaryCall<CancelInvitationRequest, CancelInvitationResponse>;
  createAccount: handleUnaryCall<CreateAccountRequest, CreateAccountResponse>;
  transferAccountOwnership: handleUnaryCall<TransferAccountOwnershipRequest, TransferAccountOwnershipResponse>;
  setDefaultAccount: handleUnaryCall<SetDefaultAccountRequest, SetDefaultAccountResponse>;
  setMembershipRoles: handleUnaryCall<SetMembershipRolesRequest, SetMembershipRolesResponse>;
  removeMembership: handleUnaryCall<RemoveMembershipRequest, RemoveMembershipResponse>;
  archiveUser: handleUnaryCall<ArchiveUserRequest, ArchiveUserResponse>;
  archiveAccount: handleUnaryCall<ArchiveAccountRequest, ArchiveAccountResponse>;
  updateUserAccountStatus: handleUnaryCall<UpdateUserAccountStatusRequest, UpdateUserAccountStatusResponse>;
  setUserServiceRoles: handleUnaryCall<SetUserServiceRolesRequest, SetUserServiceRolesResponse>;
  /** The reads. */
  getPrincipal: handleUnaryCall<GetPrincipalRequest, GetPrincipalResponse>;
  getUser: handleUnaryCall<GetUserRequest, GetUserResponse>;
  listUsers: handleUnaryCall<ListUsersRequest, ListUsersResponse>;
  searchUsersByUsername: handleUnaryCall<SearchUsersByUsernameRequest, SearchUsersByUsernameResponse>;
  getAccount: handleUnaryCall<GetAccountRequest, GetAccountResponse>;
  listAccounts: handleUnaryCall<ListAccountsRequest, ListAccountsResponse>;
  listAccountsForUser: handleUnaryCall<ListAccountsForUserRequest, ListAccountsForUserResponse>;
  getMembership: handleUnaryCall<GetMembershipRequest, GetMembershipResponse>;
  listMembershipsForUser: handleUnaryCall<ListMembershipsForUserRequest, ListMembershipsForUserResponse>;
  listAccountMembers: handleUnaryCall<ListAccountMembersRequest, ListAccountMembersResponse>;
  getInvitation: handleUnaryCall<GetInvitationRequest, GetInvitationResponse>;
  listInvitationsFromUser: handleUnaryCall<ListInvitationsFromUserRequest, ListInvitationsFromUserResponse>;
  listInvitationsForEmailAddress: handleUnaryCall<
    ListInvitationsForEmailAddressRequest,
    ListInvitationsForEmailAddressResponse
  >;
}

export interface IdentityServiceClient extends Client {
  /** The writes. */
  register(
    request: RegisterRequest,
    callback: (error: ServiceError | null, response: RegisterResponse) => void,
  ): ClientUnaryCall;
  register(
    request: RegisterRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterResponse) => void,
  ): ClientUnaryCall;
  register(
    request: RegisterRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterResponse) => void,
  ): ClientUnaryCall;
  updateProfile(
    request: UpdateProfileRequest,
    callback: (error: ServiceError | null, response: UpdateProfileResponse) => void,
  ): ClientUnaryCall;
  updateProfile(
    request: UpdateProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateProfileResponse) => void,
  ): ClientUnaryCall;
  updateProfile(
    request: UpdateProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateProfileResponse) => void,
  ): ClientUnaryCall;
  updateAccount(
    request: UpdateAccountRequest,
    callback: (error: ServiceError | null, response: UpdateAccountResponse) => void,
  ): ClientUnaryCall;
  updateAccount(
    request: UpdateAccountRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateAccountResponse) => void,
  ): ClientUnaryCall;
  updateAccount(
    request: UpdateAccountRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateAccountResponse) => void,
  ): ClientUnaryCall;
  recordAgreement(
    request: RecordAgreementRequest,
    callback: (error: ServiceError | null, response: RecordAgreementResponse) => void,
  ): ClientUnaryCall;
  recordAgreement(
    request: RecordAgreementRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RecordAgreementResponse) => void,
  ): ClientUnaryCall;
  recordAgreement(
    request: RecordAgreementRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RecordAgreementResponse) => void,
  ): ClientUnaryCall;
  invite(
    request: InviteRequest,
    callback: (error: ServiceError | null, response: InviteResponse) => void,
  ): ClientUnaryCall;
  invite(
    request: InviteRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: InviteResponse) => void,
  ): ClientUnaryCall;
  invite(
    request: InviteRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: InviteResponse) => void,
  ): ClientUnaryCall;
  acceptInvitation(
    request: AcceptInvitationRequest,
    callback: (error: ServiceError | null, response: AcceptInvitationResponse) => void,
  ): ClientUnaryCall;
  acceptInvitation(
    request: AcceptInvitationRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AcceptInvitationResponse) => void,
  ): ClientUnaryCall;
  acceptInvitation(
    request: AcceptInvitationRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AcceptInvitationResponse) => void,
  ): ClientUnaryCall;
  rejectInvitation(
    request: RejectInvitationRequest,
    callback: (error: ServiceError | null, response: RejectInvitationResponse) => void,
  ): ClientUnaryCall;
  rejectInvitation(
    request: RejectInvitationRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RejectInvitationResponse) => void,
  ): ClientUnaryCall;
  rejectInvitation(
    request: RejectInvitationRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RejectInvitationResponse) => void,
  ): ClientUnaryCall;
  cancelInvitation(
    request: CancelInvitationRequest,
    callback: (error: ServiceError | null, response: CancelInvitationResponse) => void,
  ): ClientUnaryCall;
  cancelInvitation(
    request: CancelInvitationRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CancelInvitationResponse) => void,
  ): ClientUnaryCall;
  cancelInvitation(
    request: CancelInvitationRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CancelInvitationResponse) => void,
  ): ClientUnaryCall;
  createAccount(
    request: CreateAccountRequest,
    callback: (error: ServiceError | null, response: CreateAccountResponse) => void,
  ): ClientUnaryCall;
  createAccount(
    request: CreateAccountRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateAccountResponse) => void,
  ): ClientUnaryCall;
  createAccount(
    request: CreateAccountRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateAccountResponse) => void,
  ): ClientUnaryCall;
  transferAccountOwnership(
    request: TransferAccountOwnershipRequest,
    callback: (error: ServiceError | null, response: TransferAccountOwnershipResponse) => void,
  ): ClientUnaryCall;
  transferAccountOwnership(
    request: TransferAccountOwnershipRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: TransferAccountOwnershipResponse) => void,
  ): ClientUnaryCall;
  transferAccountOwnership(
    request: TransferAccountOwnershipRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: TransferAccountOwnershipResponse) => void,
  ): ClientUnaryCall;
  setDefaultAccount(
    request: SetDefaultAccountRequest,
    callback: (error: ServiceError | null, response: SetDefaultAccountResponse) => void,
  ): ClientUnaryCall;
  setDefaultAccount(
    request: SetDefaultAccountRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetDefaultAccountResponse) => void,
  ): ClientUnaryCall;
  setDefaultAccount(
    request: SetDefaultAccountRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetDefaultAccountResponse) => void,
  ): ClientUnaryCall;
  setMembershipRoles(
    request: SetMembershipRolesRequest,
    callback: (error: ServiceError | null, response: SetMembershipRolesResponse) => void,
  ): ClientUnaryCall;
  setMembershipRoles(
    request: SetMembershipRolesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetMembershipRolesResponse) => void,
  ): ClientUnaryCall;
  setMembershipRoles(
    request: SetMembershipRolesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetMembershipRolesResponse) => void,
  ): ClientUnaryCall;
  removeMembership(
    request: RemoveMembershipRequest,
    callback: (error: ServiceError | null, response: RemoveMembershipResponse) => void,
  ): ClientUnaryCall;
  removeMembership(
    request: RemoveMembershipRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RemoveMembershipResponse) => void,
  ): ClientUnaryCall;
  removeMembership(
    request: RemoveMembershipRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RemoveMembershipResponse) => void,
  ): ClientUnaryCall;
  archiveUser(
    request: ArchiveUserRequest,
    callback: (error: ServiceError | null, response: ArchiveUserResponse) => void,
  ): ClientUnaryCall;
  archiveUser(
    request: ArchiveUserRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ArchiveUserResponse) => void,
  ): ClientUnaryCall;
  archiveUser(
    request: ArchiveUserRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ArchiveUserResponse) => void,
  ): ClientUnaryCall;
  archiveAccount(
    request: ArchiveAccountRequest,
    callback: (error: ServiceError | null, response: ArchiveAccountResponse) => void,
  ): ClientUnaryCall;
  archiveAccount(
    request: ArchiveAccountRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ArchiveAccountResponse) => void,
  ): ClientUnaryCall;
  archiveAccount(
    request: ArchiveAccountRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ArchiveAccountResponse) => void,
  ): ClientUnaryCall;
  updateUserAccountStatus(
    request: UpdateUserAccountStatusRequest,
    callback: (error: ServiceError | null, response: UpdateUserAccountStatusResponse) => void,
  ): ClientUnaryCall;
  updateUserAccountStatus(
    request: UpdateUserAccountStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateUserAccountStatusResponse) => void,
  ): ClientUnaryCall;
  updateUserAccountStatus(
    request: UpdateUserAccountStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateUserAccountStatusResponse) => void,
  ): ClientUnaryCall;
  setUserServiceRoles(
    request: SetUserServiceRolesRequest,
    callback: (error: ServiceError | null, response: SetUserServiceRolesResponse) => void,
  ): ClientUnaryCall;
  setUserServiceRoles(
    request: SetUserServiceRolesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetUserServiceRolesResponse) => void,
  ): ClientUnaryCall;
  setUserServiceRoles(
    request: SetUserServiceRolesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetUserServiceRolesResponse) => void,
  ): ClientUnaryCall;
  /** The reads. */
  getPrincipal(
    request: GetPrincipalRequest,
    callback: (error: ServiceError | null, response: GetPrincipalResponse) => void,
  ): ClientUnaryCall;
  getPrincipal(
    request: GetPrincipalRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetPrincipalResponse) => void,
  ): ClientUnaryCall;
  getPrincipal(
    request: GetPrincipalRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetPrincipalResponse) => void,
  ): ClientUnaryCall;
  getUser(
    request: GetUserRequest,
    callback: (error: ServiceError | null, response: GetUserResponse) => void,
  ): ClientUnaryCall;
  getUser(
    request: GetUserRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetUserResponse) => void,
  ): ClientUnaryCall;
  getUser(
    request: GetUserRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetUserResponse) => void,
  ): ClientUnaryCall;
  listUsers(
    request: ListUsersRequest,
    callback: (error: ServiceError | null, response: ListUsersResponse) => void,
  ): ClientUnaryCall;
  listUsers(
    request: ListUsersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListUsersResponse) => void,
  ): ClientUnaryCall;
  listUsers(
    request: ListUsersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListUsersResponse) => void,
  ): ClientUnaryCall;
  searchUsersByUsername(
    request: SearchUsersByUsernameRequest,
    callback: (error: ServiceError | null, response: SearchUsersByUsernameResponse) => void,
  ): ClientUnaryCall;
  searchUsersByUsername(
    request: SearchUsersByUsernameRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SearchUsersByUsernameResponse) => void,
  ): ClientUnaryCall;
  searchUsersByUsername(
    request: SearchUsersByUsernameRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SearchUsersByUsernameResponse) => void,
  ): ClientUnaryCall;
  getAccount(
    request: GetAccountRequest,
    callback: (error: ServiceError | null, response: GetAccountResponse) => void,
  ): ClientUnaryCall;
  getAccount(
    request: GetAccountRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetAccountResponse) => void,
  ): ClientUnaryCall;
  getAccount(
    request: GetAccountRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetAccountResponse) => void,
  ): ClientUnaryCall;
  listAccounts(
    request: ListAccountsRequest,
    callback: (error: ServiceError | null, response: ListAccountsResponse) => void,
  ): ClientUnaryCall;
  listAccounts(
    request: ListAccountsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListAccountsResponse) => void,
  ): ClientUnaryCall;
  listAccounts(
    request: ListAccountsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListAccountsResponse) => void,
  ): ClientUnaryCall;
  listAccountsForUser(
    request: ListAccountsForUserRequest,
    callback: (error: ServiceError | null, response: ListAccountsForUserResponse) => void,
  ): ClientUnaryCall;
  listAccountsForUser(
    request: ListAccountsForUserRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListAccountsForUserResponse) => void,
  ): ClientUnaryCall;
  listAccountsForUser(
    request: ListAccountsForUserRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListAccountsForUserResponse) => void,
  ): ClientUnaryCall;
  getMembership(
    request: GetMembershipRequest,
    callback: (error: ServiceError | null, response: GetMembershipResponse) => void,
  ): ClientUnaryCall;
  getMembership(
    request: GetMembershipRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetMembershipResponse) => void,
  ): ClientUnaryCall;
  getMembership(
    request: GetMembershipRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetMembershipResponse) => void,
  ): ClientUnaryCall;
  listMembershipsForUser(
    request: ListMembershipsForUserRequest,
    callback: (error: ServiceError | null, response: ListMembershipsForUserResponse) => void,
  ): ClientUnaryCall;
  listMembershipsForUser(
    request: ListMembershipsForUserRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListMembershipsForUserResponse) => void,
  ): ClientUnaryCall;
  listMembershipsForUser(
    request: ListMembershipsForUserRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListMembershipsForUserResponse) => void,
  ): ClientUnaryCall;
  listAccountMembers(
    request: ListAccountMembersRequest,
    callback: (error: ServiceError | null, response: ListAccountMembersResponse) => void,
  ): ClientUnaryCall;
  listAccountMembers(
    request: ListAccountMembersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListAccountMembersResponse) => void,
  ): ClientUnaryCall;
  listAccountMembers(
    request: ListAccountMembersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListAccountMembersResponse) => void,
  ): ClientUnaryCall;
  getInvitation(
    request: GetInvitationRequest,
    callback: (error: ServiceError | null, response: GetInvitationResponse) => void,
  ): ClientUnaryCall;
  getInvitation(
    request: GetInvitationRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetInvitationResponse) => void,
  ): ClientUnaryCall;
  getInvitation(
    request: GetInvitationRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetInvitationResponse) => void,
  ): ClientUnaryCall;
  listInvitationsFromUser(
    request: ListInvitationsFromUserRequest,
    callback: (error: ServiceError | null, response: ListInvitationsFromUserResponse) => void,
  ): ClientUnaryCall;
  listInvitationsFromUser(
    request: ListInvitationsFromUserRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListInvitationsFromUserResponse) => void,
  ): ClientUnaryCall;
  listInvitationsFromUser(
    request: ListInvitationsFromUserRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListInvitationsFromUserResponse) => void,
  ): ClientUnaryCall;
  listInvitationsForEmailAddress(
    request: ListInvitationsForEmailAddressRequest,
    callback: (error: ServiceError | null, response: ListInvitationsForEmailAddressResponse) => void,
  ): ClientUnaryCall;
  listInvitationsForEmailAddress(
    request: ListInvitationsForEmailAddressRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListInvitationsForEmailAddressResponse) => void,
  ): ClientUnaryCall;
  listInvitationsForEmailAddress(
    request: ListInvitationsForEmailAddressRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListInvitationsForEmailAddressResponse) => void,
  ): ClientUnaryCall;
}

export const IdentityServiceClient = makeGenericClientConstructor(
  IdentityServiceService,
  'primandproper.platform.identity.v1.IdentityService',
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): IdentityServiceClient;
  service: typeof IdentityServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
