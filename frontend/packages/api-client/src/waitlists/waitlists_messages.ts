// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: waitlists/waitlists_messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Timestamp } from '../google/protobuf/timestamp';

export const protobufPackage = 'waitlists';

export interface Waitlist {
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
  /**
   * closes_at is when the waitlist stops taking signups. It is never absent: a
   * list whose end is not yet decided names a far horizon, and a list that
   * should stop now is archived.
   */
  closesAt: Date | undefined;
  id: string;
  name: string;
  description: string;
}

export interface WaitlistSignup {
  createdAt: Date | undefined;
  lastUpdatedAt: Date | undefined;
  archivedAt: Date | undefined;
  /**
   * status_changed_at is when the signup last moved through the lifecycle. It is
   * not last_updated_at: editing a note changes the row without moving anybody,
   * and a reminder after an invitation is scheduled off this.
   */
  statusChangedAt: Date | undefined;
  id: string;
  waitlistId: string;
  /**
   * contact is the address the list exists to write to. It is empty for a
   * withdrawn signup, which is what a withdrawal erases.
   */
  contact: string;
  notes: string;
  /** status is one of waiting, invited, converted or withdrawn. */
  status: string;
  /**
   * subject_type and subject_id name who the signup belongs to. Both are empty
   * for a withdrawn signup, and for one that names nobody.
   */
  subjectType: string;
  subjectId: string;
}

function createBaseWaitlist(): Waitlist {
  return {
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
    closesAt: undefined,
    id: '',
    name: '',
    description: '',
  };
}

export const Waitlist: MessageFns<Waitlist> = {
  encode(message: Waitlist, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(18).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(26).fork()).join();
    }
    if (message.closesAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closesAt), writer.uint32(34).fork()).join();
    }
    if (message.id !== '') {
      writer.uint32(42).string(message.id);
    }
    if (message.name !== '') {
      writer.uint32(50).string(message.name);
    }
    if (message.description !== '') {
      writer.uint32(58).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Waitlist {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitlist();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.closesAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Waitlist {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      closesAt: isSet(object.closesAt)
        ? fromJsonTimestamp(object.closesAt)
        : isSet(object.closes_at)
          ? fromJsonTimestamp(object.closes_at)
          : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
    };
  },

  toJSON(message: Waitlist): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.closesAt !== undefined) {
      obj.closesAt = message.closesAt.toISOString();
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.name !== '') {
      obj.name = message.name;
    }
    if (message.description !== '') {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Waitlist>, I>>(base?: I): Waitlist {
    return Waitlist.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Waitlist>, I>>(object: I): Waitlist {
    const message = createBaseWaitlist();
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.closesAt = object.closesAt ?? undefined;
    message.id = object.id ?? '';
    message.name = object.name ?? '';
    message.description = object.description ?? '';
    return message;
  },
};

function createBaseWaitlistSignup(): WaitlistSignup {
  return {
    createdAt: undefined,
    lastUpdatedAt: undefined,
    archivedAt: undefined,
    statusChangedAt: undefined,
    id: '',
    waitlistId: '',
    contact: '',
    notes: '',
    status: '',
    subjectType: '',
    subjectId: '',
  };
}

export const WaitlistSignup: MessageFns<WaitlistSignup> = {
  encode(message: WaitlistSignup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(10).fork()).join();
    }
    if (message.lastUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUpdatedAt), writer.uint32(18).fork()).join();
    }
    if (message.archivedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.archivedAt), writer.uint32(26).fork()).join();
    }
    if (message.statusChangedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.statusChangedAt), writer.uint32(34).fork()).join();
    }
    if (message.id !== '') {
      writer.uint32(42).string(message.id);
    }
    if (message.waitlistId !== '') {
      writer.uint32(50).string(message.waitlistId);
    }
    if (message.contact !== '') {
      writer.uint32(58).string(message.contact);
    }
    if (message.notes !== '') {
      writer.uint32(66).string(message.notes);
    }
    if (message.status !== '') {
      writer.uint32(74).string(message.status);
    }
    if (message.subjectType !== '') {
      writer.uint32(82).string(message.subjectType);
    }
    if (message.subjectId !== '') {
      writer.uint32(90).string(message.subjectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitlistSignup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitlistSignup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.archivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.statusChangedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.waitlistId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.contact = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.subjectType = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitlistSignup {
    return {
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
          ? fromJsonTimestamp(object.created_at)
          : undefined,
      lastUpdatedAt: isSet(object.lastUpdatedAt)
        ? fromJsonTimestamp(object.lastUpdatedAt)
        : isSet(object.last_updated_at)
          ? fromJsonTimestamp(object.last_updated_at)
          : undefined,
      archivedAt: isSet(object.archivedAt)
        ? fromJsonTimestamp(object.archivedAt)
        : isSet(object.archived_at)
          ? fromJsonTimestamp(object.archived_at)
          : undefined,
      statusChangedAt: isSet(object.statusChangedAt)
        ? fromJsonTimestamp(object.statusChangedAt)
        : isSet(object.status_changed_at)
          ? fromJsonTimestamp(object.status_changed_at)
          : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      waitlistId: isSet(object.waitlistId)
        ? globalThis.String(object.waitlistId)
        : isSet(object.waitlist_id)
          ? globalThis.String(object.waitlist_id)
          : '',
      contact: isSet(object.contact) ? globalThis.String(object.contact) : '',
      notes: isSet(object.notes) ? globalThis.String(object.notes) : '',
      status: isSet(object.status) ? globalThis.String(object.status) : '',
      subjectType: isSet(object.subjectType)
        ? globalThis.String(object.subjectType)
        : isSet(object.subject_type)
          ? globalThis.String(object.subject_type)
          : '',
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
          ? globalThis.String(object.subject_id)
          : '',
    };
  },

  toJSON(message: WaitlistSignup): unknown {
    const obj: any = {};
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.lastUpdatedAt !== undefined) {
      obj.lastUpdatedAt = message.lastUpdatedAt.toISOString();
    }
    if (message.archivedAt !== undefined) {
      obj.archivedAt = message.archivedAt.toISOString();
    }
    if (message.statusChangedAt !== undefined) {
      obj.statusChangedAt = message.statusChangedAt.toISOString();
    }
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.waitlistId !== '') {
      obj.waitlistId = message.waitlistId;
    }
    if (message.contact !== '') {
      obj.contact = message.contact;
    }
    if (message.notes !== '') {
      obj.notes = message.notes;
    }
    if (message.status !== '') {
      obj.status = message.status;
    }
    if (message.subjectType !== '') {
      obj.subjectType = message.subjectType;
    }
    if (message.subjectId !== '') {
      obj.subjectId = message.subjectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaitlistSignup>, I>>(base?: I): WaitlistSignup {
    return WaitlistSignup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaitlistSignup>, I>>(object: I): WaitlistSignup {
    const message = createBaseWaitlistSignup();
    message.createdAt = object.createdAt ?? undefined;
    message.lastUpdatedAt = object.lastUpdatedAt ?? undefined;
    message.archivedAt = object.archivedAt ?? undefined;
    message.statusChangedAt = object.statusChangedAt ?? undefined;
    message.id = object.id ?? '';
    message.waitlistId = object.waitlistId ?? '';
    message.contact = object.contact ?? '';
    message.notes = object.notes ?? '';
    message.status = object.status ?? '';
    message.subjectType = object.subjectType ?? '';
    message.subjectId = object.subjectId ?? '';
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === 'string') {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
