// DO NOT EDIT.
// swift-format-ignore-file
// swiftlint:disable all
//
// Generated by the Swift generator plugin for the protocol buffer compiler.
// Source: primandproper/platform/filtering/v1/filtering.proto
//
// For information on using the generated types, please see the documentation:
//   https://github.com/apple/swift-protobuf/

/// Package primandproper.platform.filtering.v1 is the wire schema for the
/// vocabulary of a list query: which slice of a collection a caller asked for,
/// and which slice they got.
///
/// This file is shipped inside the published Go module, and it is the file
/// itself that is shipped -- not a copy for you to keep in sync. A consumer
/// puts the module's proto directory on protoc's path and imports this file by
/// its canonical name:
///
///	PLATFORM_PROTO := $(shell go list -m -f '{{.Dir}}' github.com/primandproper/platform-go/v13)/filtering/proto
///
///	protoc --proto_path proto/ --proto_path $(PLATFORM_PROTO) \
///	    --go_opt=Mprimandproper/platform/filtering/v1/filtering.proto=github.com/primandproper/platform-go/v13/filtering/filteringpb \
///	    $(CONSUMER_PROTO_FILES)   # this file deliberately absent from that list
///
/// which is how google/protobuf/timestamp.proto already works in a consumer's
/// own file: imported, not generated, linked against bindings that ship with
/// the runtime. go.mod already pins which version of this file you get, so
/// there is no protos repository to stand up and nothing to vendor.
///
/// The Go bindings are pre-generated into
/// github.com/primandproper/platform-go/v13/filtering/filteringpb, and the
/// converters between them and the Go types live beside them in
/// .../filtering/grpc. Go is the language that needs shared *code* rather than
/// shared *schema*: the page-size clamp, the default, and the cursor asymmetry
/// are server-side rules, and a second copy of one can be wrong in a way
/// nothing reports. A generated QueryFilter in Swift, TypeScript, or Kotlin is
/// a data class with eight fields and no rules to restate, so those languages
/// generate from this file and need nothing else from it.
///
/// go_package is here because this module generates from this file too. A
/// consumer generating into its own tree overrides it with the -M mapping
/// above, or with its own --go_opt, and owns its output paths entirely.
///
/// Field numbers are the compatibility promise, and they are the promise across
/// every language a consumer generates into. Numbers are never reused and never
/// repurposed: a field that goes away is reserved.

import SwiftProtobuf

// If the compiler emits an error on this type, it is because this file
// was generated by a version of the `protoc` Swift plug-in that is
// incompatible with the version of SwiftProtobuf to which you are linking.
// Please ensure that you are building against the same version of the API
// that was used to generate this file.
fileprivate struct _GeneratedWithProtocGenSwiftVersion: SwiftProtobuf.ProtobufAPIVersionCheck {
  struct _2: SwiftProtobuf.ProtobufAPIVersion_2 {}
  typealias Version = _2
}

/// QueryFilter is the request half: the slice of a collection a caller is
/// asking for.
///
/// Every field is optional and an absent one filters nothing, so the empty
/// message is a request for the default page.
///
/// The scalars carry explicit presence rather than leaning on a zero to mean
/// "unset", so a caller that set a field to its zero value stays distinguishable
/// on the wire from one that set nothing at all. What each of those means is the
/// converters' business, and .../filtering/grpc documents it.
public struct Primandproper_Platform_Filtering_V1_QueryFilter: Sendable {
  // SwiftProtobuf.Message conformance is added in an extension below. See the
  // `Message` and `Message+*Additions` files in the SwiftProtobuf library for
  // methods supported on all messages.

  /// sort_by is a direction, not a column: "asc" walks a list forwards and
  /// "desc" backwards, and nothing else. On the usual list, ordered by when a
  /// row was created, that is oldest first and newest first; a read ordered by
  /// something else -- a name search -- is walked in that order instead.
  ///
  /// It is a string rather than an enum because it is one on the other
  /// transports this type crosses -- the query parameter and the JSON body both
  /// carry those two words -- and an enum here would put a third spelling of the
  /// same two values on the wire.
  public var sortBy: String {
    get {return _sortBy ?? String()}
    set {_sortBy = newValue}
  }
  /// Returns true if `sortBy` has been explicitly set.
  public var hasSortBy: Bool {return self._sortBy != nil}
  /// Clears the value of `sortBy`. Subsequent reads from it will return its default value.
  public mutating func clearSortBy() {self._sortBy = nil}

  /// The four windows on when a row was created or last updated. Message fields
  /// already have explicit presence, so they carry no `optional` keyword; an
  /// unset one is absent exactly as an unset scalar above is.
  public var createdAfter: SwiftProtobuf.Google_Protobuf_Timestamp {
    get {return _createdAfter ?? SwiftProtobuf.Google_Protobuf_Timestamp()}
    set {_createdAfter = newValue}
  }
  /// Returns true if `createdAfter` has been explicitly set.
  public var hasCreatedAfter: Bool {return self._createdAfter != nil}
  /// Clears the value of `createdAfter`. Subsequent reads from it will return its default value.
  public mutating func clearCreatedAfter() {self._createdAfter = nil}

  public var createdBefore: SwiftProtobuf.Google_Protobuf_Timestamp {
    get {return _createdBefore ?? SwiftProtobuf.Google_Protobuf_Timestamp()}
    set {_createdBefore = newValue}
  }
  /// Returns true if `createdBefore` has been explicitly set.
  public var hasCreatedBefore: Bool {return self._createdBefore != nil}
  /// Clears the value of `createdBefore`. Subsequent reads from it will return its default value.
  public mutating func clearCreatedBefore() {self._createdBefore = nil}

  public var updatedAfter: SwiftProtobuf.Google_Protobuf_Timestamp {
    get {return _updatedAfter ?? SwiftProtobuf.Google_Protobuf_Timestamp()}
    set {_updatedAfter = newValue}
  }
  /// Returns true if `updatedAfter` has been explicitly set.
  public var hasUpdatedAfter: Bool {return self._updatedAfter != nil}
  /// Clears the value of `updatedAfter`. Subsequent reads from it will return its default value.
  public mutating func clearUpdatedAfter() {self._updatedAfter = nil}

  public var updatedBefore: SwiftProtobuf.Google_Protobuf_Timestamp {
    get {return _updatedBefore ?? SwiftProtobuf.Google_Protobuf_Timestamp()}
    set {_updatedBefore = newValue}
  }
  /// Returns true if `updatedBefore` has been explicitly set.
  public var hasUpdatedBefore: Bool {return self._updatedBefore != nil}
  /// Clears the value of `updatedBefore`. Subsequent reads from it will return its default value.
  public mutating func clearUpdatedBefore() {self._updatedBefore = nil}

  /// max_response_size is how many rows one page may hold. Absent means the
  /// default; a value above the ceiling is clamped to the ceiling rather than
  /// rejected.
  ///
  /// It is a uint32 because protobuf has no uint16, which is the type the Go
  /// field it maps to actually is. That narrowing is the reason the converters
  /// in .../filtering/grpc exist rather than each consumer writing their own: a
  /// value that narrows before it is clamped wraps instead, and 70000 becomes a
  /// legible-looking answer to a question nobody asked.
  public var maxResponseSize: UInt32 {
    get {return _maxResponseSize ?? 0}
    set {_maxResponseSize = newValue}
  }
  /// Returns true if `maxResponseSize` has been explicitly set.
  public var hasMaxResponseSize: Bool {return self._maxResponseSize != nil}
  /// Clears the value of `maxResponseSize`. Subsequent reads from it will return its default value.
  public mutating func clearMaxResponseSize() {self._maxResponseSize = nil}

  /// include_archived asks for archived rows as well. Absent leaves them out.
  public var includeArchived: Bool {
    get {return _includeArchived ?? false}
    set {_includeArchived = newValue}
  }
  /// Returns true if `includeArchived` has been explicitly set.
  public var hasIncludeArchived: Bool {return self._includeArchived != nil}
  /// Clears the value of `includeArchived`. Subsequent reads from it will return its default value.
  public mutating func clearIncludeArchived() {self._includeArchived = nil}

  /// cursor is an opaque value from a previous response. The page resumes after
  /// the row it names; absent starts at the beginning.
  public var cursor: String {
    get {return _cursor ?? String()}
    set {_cursor = newValue}
  }
  /// Returns true if `cursor` has been explicitly set.
  public var hasCursor: Bool {return self._cursor != nil}
  /// Clears the value of `cursor`. Subsequent reads from it will return its default value.
  public mutating func clearCursor() {self._cursor = nil}

  public var unknownFields = SwiftProtobuf.UnknownStorage()

  public init() {}

  fileprivate var _sortBy: String? = nil
  fileprivate var _createdAfter: SwiftProtobuf.Google_Protobuf_Timestamp? = nil
  fileprivate var _createdBefore: SwiftProtobuf.Google_Protobuf_Timestamp? = nil
  fileprivate var _updatedAfter: SwiftProtobuf.Google_Protobuf_Timestamp? = nil
  fileprivate var _updatedBefore: SwiftProtobuf.Google_Protobuf_Timestamp? = nil
  fileprivate var _maxResponseSize: UInt32? = nil
  fileprivate var _includeArchived: Bool? = nil
  fileprivate var _cursor: String? = nil
}

/// Pagination is the response half: what was applied, and where the next page
/// starts.
///
/// Its cursors are directional and are not the same cursor. previous_cursor is
/// the one that reached this page, so an empty one is the first page, and
/// cursor is the one that reaches the next. Nothing has to compare them.
public struct Primandproper_Platform_Filtering_V1_Pagination: @unchecked Sendable {
  // SwiftProtobuf.Message conformance is added in an extension below. See the
  // `Message` and `Message+*Additions` files in the SwiftProtobuf library for
  // methods supported on all messages.

  /// applied_query_filter is the filter this page was answered with, after
  /// defaults and bounds -- not necessarily the one that was sent.
  public var appliedQueryFilter: Primandproper_Platform_Filtering_V1_QueryFilter {
    get {return _storage._appliedQueryFilter ?? Primandproper_Platform_Filtering_V1_QueryFilter()}
    set {_uniqueStorage()._appliedQueryFilter = newValue}
  }
  /// Returns true if `appliedQueryFilter` has been explicitly set.
  public var hasAppliedQueryFilter: Bool {return _storage._appliedQueryFilter != nil}
  /// Clears the value of `appliedQueryFilter`. Subsequent reads from it will return its default value.
  public mutating func clearAppliedQueryFilter() {_uniqueStorage()._appliedQueryFilter = nil}

  /// cursor reaches the page after this one. It is the last row's identifier,
  /// so it is empty only when this page held no rows, and it says nothing about
  /// whether a further page exists: a full page and the final page carry an
  /// equally non-empty cursor.
  public var cursor: String {
    get {return _storage._cursor}
    set {_uniqueStorage()._cursor = newValue}
  }

  /// previous_cursor is the cursor that reached this page, echoed back from the
  /// filter that was applied. Empty means the first page.
  public var previousCursor: String {
    get {return _storage._previousCursor}
    set {_uniqueStorage()._previousCursor = newValue}
  }

  /// filtered_count is how many rows matched the filter and total_count how
  /// many were in scope regardless of it. Neither describes this page: they
  /// describe the collection it was cut from, which is why they do not shrink
  /// as a caller walks it.
  ///
  /// Both mean nothing unless counts_known is set.
  public var filteredCount: UInt64 {
    get {return _storage._filteredCount}
    set {_uniqueStorage()._filteredCount = newValue}
  }

  public var totalCount: UInt64 {
    get {return _storage._totalCount}
    set {_uniqueStorage()._totalCount = newValue}
  }

  /// max_response_size is the page size that was applied, uint32 for the same
  /// reason the request's is.
  public var maxResponseSize: UInt32 {
    get {return _storage._maxResponseSize}
    set {_uniqueStorage()._maxResponseSize = newValue}
  }

  /// counts_known reports whether the counts above were answered at all.
  ///
  /// They are plain integers, so an unanswered pair reads as 0 and 0 -- which
  /// is also what an empty collection reads as. A store whose counts ride along
  /// on its rows, handed a page that came back empty, has no row to read them
  /// off, so a caller walking a keyset to its end sees a final 0 that is not a
  /// result. This is the field that tells those apart, and false is the default
  /// so a message assembled by hand vouches for nothing until it says
  /// otherwise.
  public var countsKnown: Bool {
    get {return _storage._countsKnown}
    set {_uniqueStorage()._countsKnown = newValue}
  }

  public var unknownFields = SwiftProtobuf.UnknownStorage()

  public init() {}

  fileprivate var _storage = _StorageClass.defaultInstance
}

// MARK: - Code below here is support for the SwiftProtobuf runtime.

fileprivate let _protobuf_package = "primandproper.platform.filtering.v1"

extension Primandproper_Platform_Filtering_V1_QueryFilter: SwiftProtobuf.Message, SwiftProtobuf._MessageImplementationBase, SwiftProtobuf._ProtoNameProviding {
  public static let protoMessageName: String = _protobuf_package + ".QueryFilter"
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{3}sort_by\0\u{3}created_after\0\u{3}created_before\0\u{3}updated_after\0\u{3}updated_before\0\u{3}max_response_size\0\u{3}include_archived\0\u{1}cursor\0")

  public mutating func decodeMessage<D: SwiftProtobuf.Decoder>(decoder: inout D) throws {
    while let fieldNumber = try decoder.nextFieldNumber() {
      // The use of inline closures is to circumvent an issue where the compiler
      // allocates stack space for every case branch when no optimizations are
      // enabled. https://github.com/apple/swift-protobuf/issues/1034
      switch fieldNumber {
      case 1: try { try decoder.decodeSingularStringField(value: &self._sortBy) }()
      case 2: try { try decoder.decodeSingularMessageField(value: &self._createdAfter) }()
      case 3: try { try decoder.decodeSingularMessageField(value: &self._createdBefore) }()
      case 4: try { try decoder.decodeSingularMessageField(value: &self._updatedAfter) }()
      case 5: try { try decoder.decodeSingularMessageField(value: &self._updatedBefore) }()
      case 6: try { try decoder.decodeSingularUInt32Field(value: &self._maxResponseSize) }()
      case 7: try { try decoder.decodeSingularBoolField(value: &self._includeArchived) }()
      case 8: try { try decoder.decodeSingularStringField(value: &self._cursor) }()
      default: break
      }
    }
  }

  public func traverse<V: SwiftProtobuf.Visitor>(visitor: inout V) throws {
    // The use of inline closures is to circumvent an issue where the compiler
    // allocates stack space for every if/case branch local when no optimizations
    // are enabled. https://github.com/apple/swift-protobuf/issues/1034 and
    // https://github.com/apple/swift-protobuf/issues/1182
    try { if let v = self._sortBy {
      try visitor.visitSingularStringField(value: v, fieldNumber: 1)
    } }()
    try { if let v = self._createdAfter {
      try visitor.visitSingularMessageField(value: v, fieldNumber: 2)
    } }()
    try { if let v = self._createdBefore {
      try visitor.visitSingularMessageField(value: v, fieldNumber: 3)
    } }()
    try { if let v = self._updatedAfter {
      try visitor.visitSingularMessageField(value: v, fieldNumber: 4)
    } }()
    try { if let v = self._updatedBefore {
      try visitor.visitSingularMessageField(value: v, fieldNumber: 5)
    } }()
    try { if let v = self._maxResponseSize {
      try visitor.visitSingularUInt32Field(value: v, fieldNumber: 6)
    } }()
    try { if let v = self._includeArchived {
      try visitor.visitSingularBoolField(value: v, fieldNumber: 7)
    } }()
    try { if let v = self._cursor {
      try visitor.visitSingularStringField(value: v, fieldNumber: 8)
    } }()
    try unknownFields.traverse(visitor: &visitor)
  }

  public static func ==(lhs: Primandproper_Platform_Filtering_V1_QueryFilter, rhs: Primandproper_Platform_Filtering_V1_QueryFilter) -> Bool {
    if lhs._sortBy != rhs._sortBy {return false}
    if lhs._createdAfter != rhs._createdAfter {return false}
    if lhs._createdBefore != rhs._createdBefore {return false}
    if lhs._updatedAfter != rhs._updatedAfter {return false}
    if lhs._updatedBefore != rhs._updatedBefore {return false}
    if lhs._maxResponseSize != rhs._maxResponseSize {return false}
    if lhs._includeArchived != rhs._includeArchived {return false}
    if lhs._cursor != rhs._cursor {return false}
    if lhs.unknownFields != rhs.unknownFields {return false}
    return true
  }
}

extension Primandproper_Platform_Filtering_V1_Pagination: SwiftProtobuf.Message, SwiftProtobuf._MessageImplementationBase, SwiftProtobuf._ProtoNameProviding {
  public static let protoMessageName: String = _protobuf_package + ".Pagination"
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{3}applied_query_filter\0\u{1}cursor\0\u{3}previous_cursor\0\u{3}filtered_count\0\u{3}total_count\0\u{3}max_response_size\0\u{3}counts_known\0")

  fileprivate class _StorageClass {
    var _appliedQueryFilter: Primandproper_Platform_Filtering_V1_QueryFilter? = nil
    var _cursor: String = String()
    var _previousCursor: String = String()
    var _filteredCount: UInt64 = 0
    var _totalCount: UInt64 = 0
    var _maxResponseSize: UInt32 = 0
    var _countsKnown: Bool = false

      // This property is used as the initial default value for new instances of the type.
      // The type itself is protecting the reference to its storage via CoW semantics.
      // This will force a copy to be made of this reference when the first mutation occurs;
      // hence, it is safe to mark this as `nonisolated(unsafe)`.
      static nonisolated(unsafe) let defaultInstance = _StorageClass()

    private init() {}

    init(copying source: _StorageClass) {
      _appliedQueryFilter = source._appliedQueryFilter
      _cursor = source._cursor
      _previousCursor = source._previousCursor
      _filteredCount = source._filteredCount
      _totalCount = source._totalCount
      _maxResponseSize = source._maxResponseSize
      _countsKnown = source._countsKnown
    }
  }

  fileprivate mutating func _uniqueStorage() -> _StorageClass {
    if !isKnownUniquelyReferenced(&_storage) {
      _storage = _StorageClass(copying: _storage)
    }
    return _storage
  }

  public mutating func decodeMessage<D: SwiftProtobuf.Decoder>(decoder: inout D) throws {
    _ = _uniqueStorage()
    try withExtendedLifetime(_storage) { (_storage: _StorageClass) in
      while let fieldNumber = try decoder.nextFieldNumber() {
        // The use of inline closures is to circumvent an issue where the compiler
        // allocates stack space for every case branch when no optimizations are
        // enabled. https://github.com/apple/swift-protobuf/issues/1034
        switch fieldNumber {
        case 1: try { try decoder.decodeSingularMessageField(value: &_storage._appliedQueryFilter) }()
        case 2: try { try decoder.decodeSingularStringField(value: &_storage._cursor) }()
        case 3: try { try decoder.decodeSingularStringField(value: &_storage._previousCursor) }()
        case 4: try { try decoder.decodeSingularUInt64Field(value: &_storage._filteredCount) }()
        case 5: try { try decoder.decodeSingularUInt64Field(value: &_storage._totalCount) }()
        case 6: try { try decoder.decodeSingularUInt32Field(value: &_storage._maxResponseSize) }()
        case 7: try { try decoder.decodeSingularBoolField(value: &_storage._countsKnown) }()
        default: break
        }
      }
    }
  }

  public func traverse<V: SwiftProtobuf.Visitor>(visitor: inout V) throws {
    try withExtendedLifetime(_storage) { (_storage: _StorageClass) in
      // The use of inline closures is to circumvent an issue where the compiler
      // allocates stack space for every if/case branch local when no optimizations
      // are enabled. https://github.com/apple/swift-protobuf/issues/1034 and
      // https://github.com/apple/swift-protobuf/issues/1182
      try { if let v = _storage._appliedQueryFilter {
        try visitor.visitSingularMessageField(value: v, fieldNumber: 1)
      } }()
      if !_storage._cursor.isEmpty {
        try visitor.visitSingularStringField(value: _storage._cursor, fieldNumber: 2)
      }
      if !_storage._previousCursor.isEmpty {
        try visitor.visitSingularStringField(value: _storage._previousCursor, fieldNumber: 3)
      }
      if _storage._filteredCount != 0 {
        try visitor.visitSingularUInt64Field(value: _storage._filteredCount, fieldNumber: 4)
      }
      if _storage._totalCount != 0 {
        try visitor.visitSingularUInt64Field(value: _storage._totalCount, fieldNumber: 5)
      }
      if _storage._maxResponseSize != 0 {
        try visitor.visitSingularUInt32Field(value: _storage._maxResponseSize, fieldNumber: 6)
      }
      if _storage._countsKnown != false {
        try visitor.visitSingularBoolField(value: _storage._countsKnown, fieldNumber: 7)
      }
    }
    try unknownFields.traverse(visitor: &visitor)
  }

  public static func ==(lhs: Primandproper_Platform_Filtering_V1_Pagination, rhs: Primandproper_Platform_Filtering_V1_Pagination) -> Bool {
    if lhs._storage !== rhs._storage {
      let storagesAreEqual: Bool = withExtendedLifetime((lhs._storage, rhs._storage)) { (_args: (_StorageClass, _StorageClass)) in
        let _storage = _args.0
        let rhs_storage = _args.1
        if _storage._appliedQueryFilter != rhs_storage._appliedQueryFilter {return false}
        if _storage._cursor != rhs_storage._cursor {return false}
        if _storage._previousCursor != rhs_storage._previousCursor {return false}
        if _storage._filteredCount != rhs_storage._filteredCount {return false}
        if _storage._totalCount != rhs_storage._totalCount {return false}
        if _storage._maxResponseSize != rhs_storage._maxResponseSize {return false}
        if _storage._countsKnown != rhs_storage._countsKnown {return false}
        return true
      }
      if !storagesAreEqual {return false}
    }
    if lhs.unknownFields != rhs.unknownFields {return false}
    return true
  }
}
